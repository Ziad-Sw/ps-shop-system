# PROGRESS — PS Shop System

آخر تحديث: 2026-07-19 (تنظيف كامل — قاعدة البيانات جاهزة للإنتاج)

## ما تم بناؤه (الخطوة 1)

### مشروع Next.js
- مشروع `ps-shop-system` على Next.js 16 + React 19 + TypeScript + Tailwind CSS v4.
- هيكل المجلدات الأساسي: `app/`, `components/`, `lib/`, `types/`, `supabase/migrations/`.
- صفحة رئيسية placeholder + صفحة `/login` placeholder (بدون auth فعلي بعد).
- واجهة RTL بالعربية، Dark mode فقط.

### Design System (Tailwind tokens)
- الألوان معرّفة في `tailwind.config.ts` (ليست hex مكررة في المكوّنات):
  - `surface.page` → `#1A1C23`
  - `surface.card` → `#252830`
  - `primary` → `#00AEFF` (solid فقط — لا gradients)
  - `foreground` → `#FFFFFF`
  - `foreground.muted` → `#A0A3AC`
- لا يوجد وضع فاتح ولا `prefers-color-scheme`.

### Supabase
- `lib/supabase/client.ts` — عميل المتصفح (`@supabase/ssr`).
- `lib/supabase/server.ts` — عميل السيرفر (cookies).
- `lib/supabase/admin.ts` — عميل service role للعمليات الإدارية.
- `.env.local.example` — قالب متغيرات البيئة.

### قاعدة البيانات
- Migration: `supabase/migrations/001_initial_schema.sql`
- الجداول: `shops`, `users`, `stations`, `pricing_rules`, `shifts`, `sessions`, `products`, `sale_items`, `expenses`.
- كل جدول (ما عدا `shops`) يحمل `shop_id`.
- `start_time` / `end_time` / `opened_at` / `closed_at` من نوع `TIMESTAMPTZ` (مصدر الحقيقة = السيرفر/DB).
- Seed: صف واحد في `shops` باسم «محل البلايستيشن».
- RLS مفعّل بسياسات MVP مفتوحة مؤقتًا (تُشدَّد عند تنفيذ Auth).

### الأنواع
- `types/database.ts` — تعريفات TypeScript لكل الجداول والـ enums.

---

## قرارات برمجية

| القرار | السبب |
|---|---|
| Tailwind v4 + `@config` يشير إلى `tailwind.config.ts` | احترام طلب تعريف tokens في config وليس داخل كل component |
| `login_id` في جدول `users` بدون كلمة سر في الـ schema | توافق BRD (دخول بالمعرّف) — آلية Auth الفعلية تُحسم قبل خطوة الفريق |
| `permissions` كـ JSONB | مرونة لصلاحيات Staff دون جداول إضافية |
| `pricing_rules` على مستوى `station_type` + `mode` | يغطي تسعير PS (ساعة) وبلياردو (جيم) فردي/مالتي |
| RLS policies مفتوحة مؤقتًا | يسمح بالتطوير قبل Auth؛ يُستبدل بسياسات shop-scoped |
| لا seed لـ `users` في هذه الخطوة | المطلوب كان صف واحد فقط في `shops` |

---

## كيفية التجربة محليًا

1. انسخ `.env.local.example` إلى `.env.local` واملأ مفاتيح Supabase.
2. نفّذ migration على مشروع Supabase:
   - عبر Supabase CLI: `supabase db push`
   - أو الصق محتوى `001_initial_schema.sql` في SQL Editor.
3. `npm install` ثم `npm run dev`.
4. افتح `http://localhost:3000` — يجب أن ترى الواجهة الداكنة مع card ترحيب.

---

## الخطوة التالية (بانتظار الموافقة)

**الخطوة 2 — صفحة الإعدادات:** الأسعار، المكوّنات (PS/بلياردو)، المشروبات، عدد الورديات اليومية.

ملفات متوقعة: `app/settings/`, `app/api/products/`, `app/api/stations/`, `components/settings/`.

**ملاحظة:** تحتاج login Owner minimal أو bypass مؤقت للاختبار — يُناقش قبل البدء.

---

## ميزة تسجيل الدخول الحقيقي — مرحلة أ: القرار المعماري

تم اعتماد أن النظام لا يستخدم Supabase Auth التقليدي في تسجيل الدخول، لأن متطلب الـ MVP هو الدخول بـ `login_id` فقط بدون كلمة سر وبدون تدفق بريد/كلمة مرور.

القرار المعماري:
- إنشاء Session مخصصة داخل تطبيق Next.js.
- تمثيل الـ Session بكوكي موقّع `HttpOnly` يحمل الحد الأدنى من بيانات المستخدم:
  - `user_id`
  - `shop_id`
  - `role`
- الكوكي يجب أن يكون `HttpOnly` حتى لا يقرأه JavaScript من المتصفح، و`SameSite=Lax`، و`Secure` في بيئة الإنتاج.
- التوقيع يتم بسيرفر سيكرت من متغير بيئة خاص، حتى لا يمكن تزوير محتوى الكوكي من العميل.
- التحقق من الصلاحيات سيكون داخل API Routes والـ server-side code بناءً على محتوى الـ Session بعد التحقق من توقيعه.
- لا نعتمد على RLS مباشرة كآلية الصلاحيات الأساسية في هذه المرحلة، لأن الدخول ليس مبنيًا على مستخدم Supabase Auth. تبقى RLS الحالية مؤقتة كما هي، ويتم تضييقها لاحقًا عندما تتضح استراتيجية الأمان النهائية.

بيانات تجربة:
- يوجد مستخدم Owner تجريبي في جدول `users` بقيمة `login_id = OWNER1`.

الخطوة التالية بعد موافقة المستخدم:
- مرحلة ب: إنشاء `POST /api/auth/login` ليستقبل `login_id`، يبحث في جدول `users`، وعند التطابق ينشئ كوكي الـ Session الموقّع.

---

## ميزة تسجيل الدخول الحقيقي — مرحلة ب: API تسجيل الدخول

تم إنشاء `POST /api/auth/login`.

سلوك الـ API:
- يستقبل JSON body يحتوي على `login_id`.
- يرفض الطلب بـ `400` إذا كان جسم الطلب غير صالح أو `login_id` فارغًا.
- يبحث في جدول `users` باستخدام حقل `login_id` الموجود فعليًا في قاعدة البيانات، مع شرط `is_active = true`.
- يرجع `401` برسالة واضحة إذا لم يتم العثور على مستخدم مطابق.
- عند نجاح المطابقة، ينشئ كوكي Session موقّع `HttpOnly` باسم `ps_shop_session`.
- حمولة الـ Session الموقّعة تحتوي على:
  - `user_id`
  - `shop_id`
  - `role`
  - بيانات مساعدة للنسخة ووقت الإصدار والانتهاء.

ملفات التنفيذ:
- `lib/auth/session.ts` لإنشاء قيمة الكوكي الموقّعة وإعدادات الكوكي.
- `app/api/auth/login/route.ts` لنقطة تسجيل الدخول.

ملاحظة تنفيذية:
- توقيع الكوكي يستخدم `AUTH_SESSION_SECRET` إن وُجد، وإلا يستخدم `SUPABASE_SERVICE_ROLE_KEY` كسر سيرفر متاح حاليًا. يفضّل لاحقًا إضافة `AUTH_SESSION_SECRET` مستقل في بيئات التشغيل.

الخطوة التالية بعد موافقة المستخدم:
- مرحلة ج: إنشاء `middleware.ts` لمنع الوصول لأي صفحة غير `/login` بدون Session صالح. (تم التنفيذ)

---

## ميزة تسجيل الدخول الحقيقي — مرحلة ج: حماية المسارات (Middleware)

تم إنشاء وتفعيل `middleware.ts` لحماية كافة مسارات النظام.

سلوك الـ Middleware:
- يسمح بالوصول المفتوح إلى صفحة تسجيل الدخول `/login` والـ API الخاص بها `/api/auth/*`.
- يسمح بمرور الملفات الثابتة والصور وأصول النظام (مثل `_next/*`, `favicon.ico`).
- يتحقق من وجود كوكي الجلسة `ps_shop_session`.
- في حال وجود الكوكي، يتم التحقق من صحته وفك توقيعه رقميًا والتأكد من عدم انتهاء صلاحيته باستخدام دالة `verifySessionCookieValue` المستوردة مباشرة من `lib/auth/session.ts`.
- إذا كان الكوكي مفقودًا أو غير صالح أو منتهي الصلاحية:
  - للطلبات الموجهة للـ API (تبدأ بـ `/api/`): يرجع استجابة `401 Unauthorized` بالصيغة JSON لحماية الموارد دون التسبب في أخطاء فرونت إند.
  - للطلبات الموجهة للصفحات الأخرى: يمسح الكوكي التالف ويعيد توجيه المستخدم تلقائيًا (`307 Redirect`) إلى صفحة `/login`.

ملفات التنفيذ:
- `lib/auth/session.ts` (تمت إضافة دالتي فك ترميز Base64Url والتحقق `verifySessionCookieValue` لضمان وجود مصدر واحد للتحقق والتوقيع).
- `middleware.ts` (ملف الوسيط البرمجي الرئيسي لحماية المسارات).

الخطوة التالية بعد موافقة المستخدم:
- مرحلة د: ربط واجهة `/login` بالـ API وعرض حالة الجلسة على الصفحة الرئيسية.

---

## ميزة تسجيل الدخول الحقيقي — مرحلة د: ربط واجهة `/login` بالـ API وعرض حالة الجلسة على الصفحة الرئيسية

تم تعديل `app/page.tsx` لقراءة كوكي الجلسة `ps_shop_session` من السيرفر، والتحقق من التوقيع، ثم جلب `login_id` و`display_name` من جدول `users`.

الصفحة الرئيسية تعرض الآن رسالة شخصية للمستخدم المسجل:
- `مرحبًا ${display_name || login_id}`
- `تم تسجيل الدخول كـ ${login_id}`

كما تضيف حالة احتياطية للمستخدم غير المسجل، رغم أن `middleware.ts` يمنع الوصول إلى الصفحة الرئيسية بدون جلسة صالحة.

نتائج الاختبار:
- تسجيل دخول بـ `OWNER1` أعاد `200 OK` من `/api/auth/login` مع `Set-Cookie` صالح، وطلب `/` مع هذا الكوكي أظهر الصفحة الرئيسية بنص الترحيب.
- تسجيل دخول بـ `WRONGID` أعاد `401 Unauthorized` مع رسالة `معرّف الدخول غير صحيح.` ولم يعيد أي كوكي صالح.

> ملاحظة: `OWNER1` موجود حالياً في بيئة الاختبار كصف مدخل يدويًا عبر SQL Editor، وليس كـ seed ضمن `supabase/migrations`.

الخطوة التالية (بانتظار موافقة المستخدم):
- مرحلة هـ: تنفيذ تسجيل الخروج.

---

## ميزة تسجيل الخروج — مرحلة هـ: API وربط الواجهة

تم إضافة نقطة نهاية `POST /api/auth/logout` التي:
- تمسح كوكي الجلسة `ps_shop_session` عن طريق تعيينه بقيمة فارغة و`Max-Age=0`.
- توجه المستخدم إلى `/login` بعد الخروج.

نتائج الاختبار:
- تسجيل دخول بـ `OWNER1` ثم إرسال `POST` إلى `/api/auth/logout` أدى إلى إعادة توجيه `307` إلى `/login`.
- الكوكي تم مسحه عبر `Set-Cookie` مع `Max-Age=0` و`Expires=Thu, 01 Jan 1970 00:00:00 GMT`.
- محاولة الوصول إلى `/` بعد الخروج أعادت `307` إلى `/login` تلقائيًا بسبب `middleware.ts`.

> بعد هذه المرحلة، ميزة تسجيل الدخول مكتملة من أ إلى هـ، ويمكن الانتقال للـ feature التالي حسب BRD: صفحة الإعدادات.

---

## تحسين UX/Layout المستقل عن ميزة تسجيل الدخول — المرحلة A: shell موحّد للمستخدم المسجّل

تمت إضافة `components/layout/authenticated-shell.tsx` واستخدامه في `app/page.tsx` عند وجود جلسة صالحة.

التحسينات المطبقة:
- تم إنشاء شريط علوي ثابت (top bar) لصفحات النظام بعد تسجيل الدخول.
- يضم زر مستخدم يفتح dropdown يحتوي على:
  - اسم المستخدم الحالي / الرمز المختصر
  - رابط `الإعدادات` (تمت الإشارة إليه حتى لو لم تُبْنَ الصفحة بعد)
  - زر `تسجيل الخروج`
- لم تُستخدم هذه القشرة في صفحة `/login`، والتي تبقى منفصلة ومباشرة.
- تم الالتزام بالـ RTL وسمك العناصر القابلة للنقر بما لا يقل عن 44×44px.
- تم استخدام ألوان Design System الحالية دون gradients.

النتيجة:
- الصفحة الرئيسية بعد تسجيل الدخول تستخدم الآن shell موحّد بدل زر تسجيل الخروج البارز في منتصف الصفحة.
- صفحة `/login` لا تستخدم هذا الـ shell.

---

## تحديث الشريط العلوي والإعدادات — المرحلة B: اسم المحل الديناميكي والتعديل

تم تحديث `components/layout/authenticated-shell.tsx` لتطبيق قرار معماري من القسم 6.5 في `TECH_INSTRUCTIONS.md`:

**التغييرات:**
1. الشريط العلوي الآن ينقسم إلى جزأين:
   - **الشمال (ثابت)**: "PS-System" — هوية المنتج الثابتة، نص غير تفاعلي بدون dropdown
   - **اليمين (ديناميكي)**: اسم المحل (`shops.name`) مع سهم ▾ يشير إلى dropdown، وهو الزرار الوحيد الذي يفتح القائمة المنسدلة

2. تم استبدال زرار المستخدم (الذي كان يعرض avatar + اسم) بزرار اسم المحل مع السهم

3. محتوى القائمة المنسدلة بقي كما هو:
   - اسم المستخدم (`display_name`)
   - معرّف الدخول (`login_id`)
   - رابط "الإعدادات"
   - زرار "تسجيل الخروج"

4. تم تعديل `app/page.tsx`:
   - إضافة دالة `getShopName()` لجلب اسم المحل من جدول `shops`
   - تمرير `shopName` كـ prop جديد إلى `AuthenticatedShell`

5. تم إنشاء صفحة `/app/settings/page.tsx`:
   - Server component تجلب بيانات المستخدم واسم المحل
   - تعرض `AuthenticatedShell` مع محتوى الإعدادات

6. تم إنشاء مكون `components/settings/settings-form.tsx`:
   - Client component بـ form لتعديل اسم المحل
   - تحديث اسم المحل يحفظ تغييرات فورية

7. تم إنشاء API endpoint `POST /api/shops/update`:
   - يستقبل JSON مع `name` جديد
   - يتحقق من الجلسة والصلاحيات
   - يحدّث جدول `shops` مع اسم المحل الجديد
   - يرجع رسالة النجاح أو الخطأ

**المنطق المعماري:**
- اسم المحل وبيانات الحساب/الجلسة (المستخدم، الإعدادات، الخروج) مرتبطين منطقيًا ببعض (سياق الجلسة الحالية) → يجب تجميعهم على نفس الناحية (اليمين)
- "PS-System" هوية المنتج المجردة تبقى منفصلة على الشمال كعنصر ثابت لا يتغير مع السياق

**نتائج الاختبار:**
✅ تسجيل دخول بـ OWNER1 يعرض الشريط العلوي مع "PS-System" على الشمال و "محل البلايستيشن" على اليمين
✅ القائمة المنسدلة تفتح عند الضغط على اسم المحل
✅ رابط الإعدادات يوجه إلى `/settings` 
✅ صفحة الإعدادات تحمّل بنجاح مع نفس الـ shell
✅ تعديل اسم المحل من "محل البلايستيشن" إلى "محل جديد للبلايستيشن" وحفظه يعدّل الاسم في:
  - حقل الإدخال في صفحة الإعدادات
  - الشريط العلوي فورًا عند العودة للصفحة الرئيسية
  - جدول `shops` في قاعدة البيانات

**ملفات التنفيذ:**
- `components/layout/authenticated-shell.tsx` — تحديث الشريط العلوي والقائمة المنسدلة
- `app/page.tsx` — جلب اسم المحل وتمريره للـ shell
- `app/settings/page.tsx` — صفحة جديدة للإعدادات
- `components/settings/settings-form.tsx` — مكون النموذج لتعديل إعدادات المحل
- `app/api/shops/update/route.ts` — API endpoint لحفظ تعديلات المحل

**Commit:** 82190fa - "feat: update top bar with shop name and create settings page"

**تحديث 1:** تم عكس اتجاهات الشريط العلوي (e093d42):
- PS-System الآن على اليسار (ثابت)
- اسم المحل مع dropdown الآن على اليمين (ديناميكي)

**تحديث 2:** تم تحديث موضع الـ dropdown menu (87661e4):
- الـ dropdown الآن موضوع من اليمين (`right-0`) في كود CSS
- جميع اتجاهات الـ dropdown (النصوص والعناصر) من ناحية اليمين (RTL)

الخطوة التالية (بانتظار موافقة المستخدم):
- المرحلة C: إضافة الإعدادات الأخرى (تفعيل/تعطيل المكوّنات، عدد الورديات، الأسعار، المشروبات)

---

## تطبيق الخط الرسمي — IBM Plex Sans Arabic

تم استبدال خط Geist الافتراضي بـ **IBM Plex Sans Arabic** كخط رسمي شامل للنظام (عربي + إنجليزي/أرقام)، وفقًا لقرار موثق في `TECH_INSTRUCTIONS.md` قسم 6.

**التغييرات:**
1. `app/layout.tsx`:
   - استبدال `Geist` و`Geist_Mono` بـ `IBM_Plex_Sans_Arabic` من `next/font/google`.
   - تحميل الأوزان: `400` (Regular)، `500` (Medium)، `600` (SemiBold)، `700` (Bold).
   - تحميل المجموعتين: `arabic` و`latin`.
   - CSS variable: `--font-ibm-plex-arabic`.

2. `app/globals.css`:
   - `body` يستخدم `var(--font-ibm-plex-arabic), sans-serif` بدل `var(--font-geist-sans), Arial, Helvetica, sans-serif`.

3. تم التحقق من عدم وجود أي مرجع لخط Geist أو خط آخر في أي ملف من ملفات المشروع (`app/`, `components/`, `lib/`).

4. تم التحقق من نجاح البناء (`next build`) بدون أخطاء TypeScript أو compilation.

**ملفات التنفيذ:**
- `app/layout.tsx` — تحميل الخط وتطبيقه على `<html>`
- `app/globals.css` — تعريف الخط كافتراضي لـ `body`

---

## إعادة هيكلة صفحة الإعدادات — المرحلة C: 5 كروت إعدادات منفصلة

تم إعادة هيكلة صفحة `/settings` بالكامل لتقسيم الإعدادات إلى 5 كروت منفصلة مع صفحات فرعية مخصصة لكل منها.

**التغييرات:**

1. **صفحات فرعية جديدة:**
   - `/settings/ps` — إدارة إعدادات البلايستيشن (تفعيل/تعطيل + أسعار فردي/مالتي)
   - `/settings/billiard` — إدارة إعدادات البلياردو (تفعيل/تعطيل + أسعار فردي/مالتي)
   - `/settings/products` — إدارة المشروبات (إضافة/تعديل/حذف/تفعيل)
   - `/settings/shifts` — إعدادات الورديات (عدد الورديات اليومية)

2. **مكونات النماذج الجديدة:**
   - `components/settings/ps-settings-form.tsx` — نموذج إعدادات البلايستيشن
   - `components/settings/billiard-settings-form.tsx` — نموذج إعدادات البلياردو
   - `components/settings/products-settings-form.tsx` — نموذج إدارة المشروبات
   - `components/settings/shifts-settings-form.tsx` — نموذج إعدادات الورديات

3. **تحديث الصفحة الرئيسية للإعدادات:**
   - `components/settings/settings-form.tsx` — تحويل الـ placeholders إلى كروت روابط قابلة للنقر
   - 5 كروت: اسم المحل (تعديل مباشر)، البلايستيشن، البلياردو، المشروبات، الورديات

4. **ربط API الموجود:**
   - `/api/shops/update` — تحديث اسم المحل، تفعيل/تعطيل PS والبلياردو، عدد الورديات
   - `/api/pricing-rules/upsert` — تحديث أسعار PS والبلياردو
   - `/api/products` — إضافة مشروبات جديدة
   - `/api/products/[id]` — تعديل/حذف المشروبات

5. **تحسينات UX في صفحة المشروبات:**
   - إضافة زر "تعديل" بدلاً من التعديل المباشر
   - عند الضغط على "تعديل": تتحول الحقول لقابلة للتعديل
   - أزرار "حفظ" و "إلغاء" تظهر أثناء التعديل
   - زر "مفعّل/معطّل" منفصل للتحكم في حالة المشروب

6. **تحسينات في حقول الأسعار:**
   - تغيير `step` من `0.01` إلى `1` في جميع حقول الأسعار (PS، البلياردو، المشروبات)
   - التغيير بالأسهم (▲/▼) الآن بـ 1 جنيه بدلاً من 0.10 جنيه
   - إمكانية الكتابة اليدوية لأي سعر

7. **تحسينات في صفحات الأسعار (PS و Billiard):**
   - كل input له زر حفظ خاص به (يحفظ السعر الخاص به فقط)
   - زر "حفظ جميع الأسعار" في الأسفل (يحفظ جميع الأسعار معًا)
   - إضافة `savingMode` state لإظهار "جاري..." فقط على الزر المضغوط

8. **تحسينات في صفحة الورديات:**
   - تغيير الحد الأقصى من 3 إلى 4 ورديات يوميًا
   - تحديث النصوص التوضيحية والتحقق في الكود
   - تحسين رسالة الخطأ عند محاولة التعديل أثناء وجود وردية مفتوحة:
     - "لا يمكن تعديل هذه الإعدادات أثناء وجود وردية مفتوحة. يمكنك التعديل فقط بين الورديات أو عندما لا تكون هناك وردية شغالة."

9. **إصلاحات TypeScript:**
   - تحديث نوع `update` في `/api/shops/update/route.ts` من `Record<string, unknown>` إلى `Partial<{...}>`
   - تحديث نوع `update` في `/api/products/[id]/route.ts` من `Record<string, unknown>` إلى `Partial<{...}>`
   - إصلاح `getPricingRules` في صفحات PS و Billiard من `maybeSingle()` إلى استعلام عادي (لأنه يرجع array)

10. **Migration جديد:**
    - `supabase/migrations/002_update_shifts_per_day_max.sql` — تحديث constraint من 1-3 إلى 1-4

**ملفات التنفيذ:**
- `app/settings/ps/page.tsx` — صفحة إعدادات البلايستيشن
- `app/settings/billiard/page.tsx` — صفحة إعدادات البلياردو
- `app/settings/products/page.tsx` — صفحة إدارة المشروبات
- `app/settings/shifts/page.tsx` — صفحة إعدادات الورديات
- `components/settings/ps-settings-form.tsx` — نموذج إعدادات البلايستيشن
- `components/settings/billiard-settings-form.tsx` — نموذج إعدادات البلياردو
- `components/settings/products-settings-form.tsx` — نموذج إدارة المشروبات
- `components/settings/shifts-settings-form.tsx` — نموذج إعدادات الورديات
- `components/settings/settings-form.tsx` — تحديث الكروت الرئيسية
- `app/api/shops/update/route.ts` — تحديث validation ورسائل الخطأ
- `app/api/products/[id]/route.ts` — إصلاح TypeScript types
- `supabase/migrations/002_update_shifts_per_day_max.sql` — migration جديد

**نتائج الاختبار:**
✅ `next build` ناجح بدون أخطاء
✅ جميع الصفحات الفرعية تفتح بشكل صحيح
✅ الأزرار والنماذج تعمل كما متوقع
✅ رسائل الخطأ واضحة ومفيدة للمستخدم

**ملاحظة:**
يجب تطبيق migration `002_update_shifts_per_day_max.sql` يدويًا على Supabase قبل استخدام صفحة الورديات.

الخطوة التالية (مؤجلة):
- إضافة إمكانية التحكم في ساعات الورديات (تم تأجيلها بناءً على طلب المستخدم)

---

## تحسينات الشريط العلوي — المرحلة D: إضافة أيقونة الهوم وتحسين محاذاة الدروب داون

تم تحديث `components/layout/authenticated-shell.tsx` لتحسين تجربة المستخدم في الشريط العلوي.

**التغييرات:**

1. **إضافة أيقونة الهوم:**
   - إضافة أيقونة منزل (Home icon) بجانب الدروب داون على اليمين
   - الأيقونة عبارة عن SVG من Lucide icons
   - مرتبطة بـ Link توجه إلى الصفحة الرئيسية `/`
   - تظهر في جميع الصفحات لأنها في `authenticated-shell`
   - لها نفس التصميم (border, hover states, focus ring) مثل زر اسم المحل
   - تحتوي على `title="الرئيسية"` للوصول

2. **تحسين محاذاة النص في الدروب داون:**
   - إضافة `text-right` إلى container العناصر في الدروب داون
   - جميع النصوص الآن محاذاة لليمين بشكل صحيح
   - يشمل: اسم المستخدم، معرّف الدخول، الإعدادات، تسجيل الخروج
   - الدروب داون يظل في نفس المكان (right-0) لكن المحتوى محاذاة لليمين

3. **الهيكل الجديد للشريط العلوي:**
   - اليسار: "PS-System" (ثابت)
   - اليمين: flex container يحتوي على:
     - أيقونة الهوم (جديدة)
     - زر اسم المحل مع الدروب داون

**ملفات التنفيذ:**
- `components/layout/authenticated-shell.tsx` — إضافة أيقونة الهوم وتحسين محاذاة الدروب داون

**نتائج الاختبار:**
✅ أيقونة الهوم تظهر بجانب الدروب داون
✅ عند الضغط على الأيقونة، يتم التوجيه إلى الصفحة الرئيسية
✅ جميع النصوص في الدروب داون محاذاة لليمين
✅ الدروب داون يظل في نفس المكان (right-0)
✅ التصميم متسق مع باقي عناصر الشريط العلوي

---

## تحسينات الشريط العلوي — المرحلة E: تحسين زر تسجيل الخروج وتحديث محتوى الدروب داون

تم تحديث `components/layout/authenticated-shell.tsx` لتحسين تجربة المستخدم والأمان.

**التغييرات:**

1. **تحسين زر تسجيل الخروج:**
   - تغيير اللون إلى الأحمر (`text-red-400`) لتمييزه كإجراء خطير (UX best practice)
   - إضافة خلفية حمراء عند التمرير (`hover:bg-red-500/10`)
   - تغيير من form submit إلى button مع onClick handler
   - إضافة رسالة تأكيد قبل التنفيذ: "هل أنت متأكد من تسجيل الخروج؟"

2. **تحديث محتوى الدروب داون للأمان:**
   - إخفاء معرّف الدخول (`login_id`) من الدروب داون للأمان
   - السطر الأول الآن يعرض اسم صاحب المحل (`display_name`) أو "صاحب المحل" كـ fallback
   - السطر الثاني يعرض اسم المحل بدلاً من المعرّف

**ملفات التنفيذ:**
- `components/layout/authenticated-shell.tsx` — تحسين زر تسجيل الخروج وتحديث محتوى الدروب داون

**نتائج الاختبار:**
✅ زر تسجيل الخروج باللون الأحمر
✅ رسالة تأكيد تظهر قبل تسجيل الخروج
✅ معرّف الدخول مخفي من الدروب داون
✅ اسم صاحب المحل يظهر في السطر الأول
✅ اسم المحل يظهر في السطر الثاني

---

## إضافة حقل اسم صاحب المحل — المرحلة F: إضافة owner_name وكارد الإعدادات

تم إضافة حقل `owner_name` إلى جدول `shops` وكارد إعدادات لتعديله.

**التغييرات:**

1. **قاعدة البيانات:**
   - إضافة عمود `owner_name` إلى جدول `shops` (Migration 003)
   - تحديث TypeScript types في `types/database.ts`

2. **API:**
   - تحديث `/api/shops/update` لقبول حقل `owner_name`
   - إضافة validation للتحقق من أن الحقل ليس فارغًا
   - الحقل معفى من shift-lock (يمكن تعديله في أي وقت)

3. **واجهة الإعدادات:**
   - إضافة كارد "اسم صاحب المحل" في `settings-form.tsx`
   - الكارد تحت كارد "اسم المحل" بنفس الهيكل
   - زر حفظ مستقل لكل كارد (loading states منفصلة)
   - حفظ فوري عند الضغط على الزر

4. **تحديث جميع الصفحات:**
   - إضافة دالة `getOwnerName` في جميع صفحات الإعدادات والصفحة الرئيسية
   - تمرير `ownerName` كـ prop جديد إلى `AuthenticatedShell`
   - تحديث `AuthenticatedShell` لاستخدام `ownerName` في الدروب داون

5. **الربط:**
   - الدروب داون يعرض الآن `ownerName` من قاعدة البيانات
   - عند تعديل اسم صاحب المحل في الإعدادات، يتحديث فوراً في الدروب داون

**ملفات التنفيذ:**
- `supabase/migrations/003_add_owner_name_to_shops.sql` — migration جديد
- `types/database.ts` — تحديث types
- `app/api/shops/update/route.ts` — تحديث API
- `components/settings/settings-form.tsx` — إضافة كارد اسم صاحب المحل
- `app/page.tsx` — إضافة getOwnerName وتمرير ownerName
- `app/settings/page.tsx` — إضافة getOwnerName وتمرير ownerName
- `app/settings/ps/page.tsx` — إضافة getOwnerName وتمرير ownerName
- `app/settings/billiard/page.tsx` — إضافة getOwnerName وتمرير ownerName
- `app/settings/products/page.tsx` — إضافة getOwnerName وتمرير ownerName
- `app/settings/shifts/page.tsx` — إضافة getOwnerName وتمرير ownerName
- `components/layout/authenticated-shell.tsx` — تحديث لاستخدام ownerName

**نتائج الاختبار:**
✅ Migration تم تطبيقه بنجاح على Supabase
✅ كارد "اسم صاحب المحل" يظهر في صفحة الإعدادات
✅ حفظ اسم صاحب المحل يعمل بشكل صحيح
✅ الدروب داون يعرض اسم صاحب المحل من قاعدة البيانات
✅ التحديث فوري في الدروب داون بعد الحفظ

---

## تحسينات صفحة تسجيل الدخول — المرحلة G: تحسين UX writing واستبدال alert

تم تحسين صفحة تسجيل الدخول لتحسين تجربة المستخدم واستبدال رسائل المتصفح برسائل النظام.

**التغييرات:**

1. **تحسين UX Writing:**
   - تغيير العنوان الرئيسي إلى "PS-System" (مثل navbar)
   - تغيير العنوان الفرعي إلى "نظام إدارة المحل"
   - تغيير label من "معرف الدخول" إلى "تسجيل الدخول"
   - تغيير placeholder من "مثال: OWNER1" إلى "أدخل معرف الدخول الخاص بك"
   - تغيير نص الزر من "تسجيل الدخول" إلى "دخول"
   - تغيير نص التحميل من "جاري الدخول..." إلى "جاري تسجيل الدخول..."

2. **استبدال alert بـ toast:**
   - إضافة ToastProvider إلى صفحة تسجيل الدخول
   - استبدال alert برسالة toast للدعم عند الضغط على "هل نسيت ال-ID؟"
   - الرسالة: "إذا نسيت معرف الدخول، يرجى التواصل مع الدعم للحصول على معرف جديد."

3. **تحسين اتجاه الإدخال:**
   - إزالة `dir="ltr"` من input لاستخدام RTL بشكل افتراضي
   - النص الآن يظهر من اليمين لليسار

**ملفات التنفيذ:**
- `app/(auth)/login/page.tsx` — تحسين UX writing واستبدال alert بـ toast

**نتائج الاختبار:**
✅ اسم النظام PS-System يظهر في صفحة تسجيل الدخول
✅ النصوص أكثر وضوحاً للمستخدم
✅ رابط "هل نسيت ال-ID؟" يظهر toast message من النظام
✅ النص في input يظهر من اليمين (RTL)

---

## تحسينات عامة — المرحلة H: توحيد نظام الإشعارات والخطوط

تم توحيد نظام الإشعارات في جميع أنحاء التطبيق والتأكد من توحيد الخطوط.

**التغييرات:**

1. **توحيد نظام الإشعارات (Toast):**
   - إضافة نوع "info" إلى نظام toast (بجانب success و error)
   - تحديث Toast component لدعم النوع الجديد مع تصميم أزرق
   - استبدال confirm() في logout بـ toast message
   - إعادة هيكلة AuthenticatedShell لاستخدام ToastProvider بشكل صحيح
   - إنشاء AuthenticatedShellContent داخلي لاستخدام useToast hook

2. **التأكد من توحيد الخطوط:**
   - التحقق من أن IBM Plex Sans Arabic مطبق في layout.tsx
   - الخط مطبق على مستوى html element مع variable
   - جميع المكونات ترث الخط تلقائياً

**ملفات التنفيذ:**
- `components/ui/toast.tsx` — إضافة نوع info وتحديث التصميم
- `components/layout/authenticated-shell.tsx` — استبدال confirm بـ toast وإعادة الهيكلة

**نتائج الاختبار:**
✅ جميع الإشعارات تستخدم نظام toast الموحد
✅ لا توجد رسائل من المتصفح (alert/confirm)
✅ نوع info يعمل بشكل صحيح مع تصميم أزرق
✅ الخط IBM Plex Sans Arabic موحد في جميع الصفحات

---

## تحسينات نظام الإشعارات — المرحلة I: إضافة تأكيد الخروج وتحسين الألوان

تم تحسين نظام الإشعارات لإضافة تأكيد للإجراءات الخطيرة وتحسين الألوان.

**التغييرات:**

1. **إضافة نوع warning:**
   - إضافة نوع "warning" إلى نظام toast
   - نوع warning يستخدم نفس تصميم error (أحمر) للإجراءات الخطيرة
   - تمييز الإجراءات الخطيرة عن الإشعارات العادية

2. **إضافة modal تأكيد تسجيل الخروج:**
   - عند الضغط على تسجيل الخروج، يظهر modal تأكيد
   - Modal يحتوي على زر "إلغاء" وزر "تسجيل الخروج"
   - Modal مع backdrop blur وتصميم مناسب
   - عند التأكيد، يظهر toast warning باللون الأحمر

3. **تحديث رسائل الإجراءات الخطيرة:**
   - تغيير "هل نسيت ال-ID؟" من info إلى warning (أحمر)
   - تغيير رسالة تسجيل الخروج إلى warning (أحمر)
   - الإجراءات الخطيرة الآن تظهر باللون الأحمر بشكل واضح

4. **الصوت:**
   - الصوت يعمل فقط للإشعارات من نوع success
   - الإجراءات الخطيرة (warning/error) لا تعمل صوت

**ملفات التنفيذ:**
- `components/ui/toast.tsx` — إضافة نوع warning وتحديث التصميم
- `components/layout/authenticated-shell.tsx` — إضافة modal تأكيد الخروج
- `app/(auth)/login/page.tsx` — تغيير forgot ID إلى warning

**نتائج الاختبار:**
✅ modal تأكيد تسجيل الخروج يظهر بشكل صحيح
✅ زر إلغاء يعمل بشكل صحيح
✅ رسائل warning تظهر باللون الأحمر
✅ الصوت يعمل للإشعارات success فقط

---

## إضافة البينغ بونغ وتحسين إعدادات الأجهزة — المرحلة J: إضافة عدد الأجهزة/الطرابيز والبينغ بونغ

تم إضافة نظام البينغ بونغ كخدمة جديدة مع إضافة حقول عدد الأجهزة والطرابيز لجميع الخدمات.

**التغييرات:**

1. **قاعدة البيانات:**
   - إضافة عمود `ps_station_count` لعدد أجهزة البلايستيشن
   - إضافة عمود `billiard_table_count` لعدد طاولات البلياردو
   - إضافة عمود `pingpong_table_count` لعدد طاولات البينغ بونغ
   - جميع الأعمدة بقيمة افتراضية 0

2. **Types:**
   - إضافة "pingpong" إلى StationType
   - تحديث Row/Insert/Update لجدول shops بالحقول الجديدة

3. **API:**
   - تحديث `/api/shops/update` لقبول الحقول الجديدة
   - إضافة validation للحقول الجديدة (>= 0)
   - إضافة الحقول الجديدة إلى shift-lock check
   - تحديث select query لإرجاع الحقول الجديدة

4. **صفحة إعدادات البلايستيشن:**
   - إضافة قسم "عدد الأجهزة"
   - إضافة input لتحديد عدد الأجهزة
   - إضافة دالة handleSaveStationCount
   - تحديث props لاستقبال initialStationCount

5. **صفحة إعدادات البلياردو:**
   - إضافة قسم "عدد الطاولات"
   - إضافة input لتحديد عدد الطاولات
   - إضافة دالة handleSaveTableCount
   - تحديث props لاستقبال initialTableCount

6. **صفحة إعدادات البينغ بونغ (جديدة):**
   - إنشاء `components/settings/pingpong-settings-form.tsx`
   - إنشاء `app/settings/pingpong/page.tsx`
   - نفس هيكل صفحة البلياردو مع:
     - تفعيل/تعطيل البينغ بونغ
     - عدد الطاولات
     - أسعار اللعب الفردي والمالتي
   - استخدام station_type = "pingpong"

7. **صفحة الإعدادات الرئيسية:**
   - إضافة كارد البينغ بونغ تحت كارد البلياردو
   - رابط إلى `/settings/pingpong`

**ملفات التنفيذ:**
- `supabase/migrations/004_add_station_counts_to_shops.sql` — إضافة أعمدة عدد الأجهزة/الطرابيز
- `types/database.ts` — تحديث types بالحقول الجديدة
- `app/api/shops/update/route.ts` — تحديث API
- `components/settings/ps-settings-form.tsx` — إضافة عدد الأجهزة
- `app/settings/ps/page.tsx` — تمرير initialStationCount
- `components/settings/billiard-settings-form.tsx` — إضافة عدد الطاولات
- `app/settings/billiard/page.tsx` — تمرير initialTableCount
- `components/settings/pingpong-settings-form.tsx` — إنشاء (جديد)
- `app/settings/pingpong/page.tsx` — إنشاء (جديد)
- `components/settings/settings-form.tsx` — إضافة كارد البينغ بونغ

**نتائج الاختبار:**
✅ Migration تم إنشاؤه بنجاح
✅ Types تم تحديثها
✅ API يقبل الحقول الجديدة
✅ صفحة البلايستيشن تحتوي على قسم عدد الأجهزة
✅ صفحة البلياردو تحتوي على قسم عدد الطاولات
✅ صفحة البينغ بونغ تم إنشاؤها بنجاح
✅ كارد البينغ بونغ يظهر في صفحة الإعدادات الرئيسية

---

## إصلاح الأخطاء وتحسين UX Writing — المرحلة K: إصلاح صفحات الإعدادات وتحسين رسائل الخطأ

تم إصلاح الأخطاء في صفحات الإعدادات وتحسين رسائل الخطأ لتكون أكثر وضوحاً للمستخدم.

**التغييرات:**

1. **إصلاح خطأ صفحة البينغ بونغ:**
   - إضافة عمود `pingpong_enabled` إلى جدول shops
   - تحديث types/database.ts لإضافة pingpong_enabled
   - تحديث API لقبول pingpong_enabled
   - تحديث صفحة pingpong لاستخدام pingpong_enabled بدلاً من billiard_enabled
   - تحديث pingpong-settings-form لاستخدام pingpong_enabled

2. **تحسين UX Writing لرسائل الخطأ:**
   - تحديث shifts-settings-form لإظهار رسالة واضحة عند وجود وردية مفتوحة
   - تحديث ps-settings-form لإظهار رسالة واضحة عند وجود وردية مفتوحة (للتفعيل وعدد الأجهزة)
   - تحديث billiard-settings-form لإظهار رسالة واضحة عند وجود وردية مفتوحة (للتفعيل وعدد الطاولات)
   - تحديث pingpong-settings-form لإظهار رسالة واضحة عند وجود وردية مفتوحة (للتفعيل وعدد الطاولات)
   - الرسائل الجديدة توضح المشكلة وتحدد الخطوة التالية (إغلاق الوردية أولاً)

**ملفات التنفيذ:**
- `supabase/migrations/004_add_station_counts_to_shops.sql` — إضافة pingpong_enabled
- `types/database.ts` — إضافة pingpong_enabled
- `app/api/shops/update/route.ts` — إضافة pingpong_enabled
- `app/settings/pingpong/page.tsx` — استخدام pingpong_enabled
- `components/settings/pingpong-settings-form.tsx` — استخدام pingpong_enabled وتحسين الرسائل
- `components/settings/ps-settings-form.tsx` — تحسين رسائل الخطأ
- `components/settings/billiard-settings-form.tsx` — تحسين رسائل الخطأ
- `components/settings/shifts-settings-form.tsx` — تحسين رسائل الخطأ

**نتائج الاختبار:**
✅ صفحة البينغ بونغ تعمل بشكل صحيح
✅ صفحة البلايستيشن تعمل بشكل صحيح
✅ صفحة البلياردو تعمل بشكل صحيح
✅ رسائل الخطأ واضحة وتحدد المشكلة والحل
✅ UX Writing محسّن لجميع رسائل الخطأ
✅ Migration تم تشغيله بنجاح على قاعدة البيانات

---

## إصلاح مشاكل البينغ بونغ — المرحلة L: إصلاح حفظ الأسعار وتفعيل افتراضي

تم إصلاح مشكلتين في صفحة البينغ بونغ.

**التغييرات:**

1. **إصلاح خطأ حفظ الأسعار:**
   - تحديث API `/api/pricing-rules/upsert` لقبول station_type = "pingpong"
   - إضافة "pingpong" إلى validation في API

2. **تغيير القيمة الافتراضية:**
   - تغيير pingpong_enabled من false إلى true في migration
   - الآن البينغ بونغ مفعّل افتراضياً في النظام

**ملفات التنفيذ:**
- `app/api/pricing-rules/upsert/route.ts` — إضافة pingpong إلى validation
- `supabase/migrations/004_add_station_counts_to_shops.sql` — تغيير القيمة الافتراضية إلى true

**نتائج الاختبار:**
✅ حفظ الأسعار في البينغ بونغ يعمل بشكل صحيح
✅ البينغ بونغ مفعّل افتراضياً

---

## إصلاح constraint في قاعدة البيانات — المرحلة M: إضافة pingpong إلى station_type check

تم إصلاح CHECK constraint في قاعدة البيانات لقبول pingpong.

**التغييرات:**

1. **إنشاء migration جديد:**
   - إضافة pingpong إلى pricing_rules station_type check
   - إضافة pingpong إلى stations station_type check
   - إسقاط وإعادة إنشاء الـ constraints

**ملفات التنفيذ:**
- `supabase/migrations/005_add_pingpong_to_station_type_check.sql` — migration جديد

**نتائج الاختبار:**
✅ Migration تم تشغيله بنجاح
✅ حفظ أسعار البينغ بونغ يعمل الآن

---

## إصلاح رسائل الخطأ في البينغ بونغ — المرحلة N: تحسين معالجة الأخطاء

تم إصلاح معالجة الأخطاء في صفحة البينغ بونغ لتظهر رسائل واضحة بالعربية بدلاً من رسائل عامة.

**التغييرات:**

1. **إصلاح handleSavePricing:**
   - تغيير throw Error إلى showToast مباشرة
   - عرض رسالة الخطأ من API بالعربية

2. **إصلاح handleSaveAllPricing:**
   - تغيير throw Error إلى showToast مباشرة
   - عرض رسالة الخطأ من API بالعربية

**ملفات التنفيذ:**
- `components/settings/pingpong-settings-form.tsx` — تحسين معالجة الأخطاء

**نتائج الاختبار:**
✅ رسائل الخطأ تظهر بالعربية بشكل واضح
✅ حفظ الأسعار يعمل بشكل صحيح

---

## إضافة اللوجو — المرحلة O: إضافة اللوجو في النافبار والتاب

تم إضافة اللوجو الرسمي للنظام في مكانين: بجانب اسم المشروع في النافبار وكـ favicon للمتصفح.

**التغييرات:**

1. **إضافة اللوجو في النافبار:**
   - إضافة `<img src="/logo-ps.svg">` بجانب "PS-System" في الشريط العلوي
   - اللوجو يظهر على يسار كلمة "PS-System" (بسبب RTL)
   - حجم اللوجو: 32x32px (h-8 w-8)

2. **إضافة اللوجو كـ favicon:**
   - تحديث metadata في `app/layout.tsx`
   - إضافة `icons: { icon: "/logo-ps.svg" }`
   - اللوجو يظهر في التاب في المتصفح

3. **نقل الملف:**
   - نسخ اللوجو من `assets/logo-ps.svg` إلى `public/logo-ps.svg`
   - الملف متاح الآن للمتصفح

**ملفات التنفيذ:**
- `components/layout/authenticated-shell.tsx` — إضافة اللوجو في النافبار
- `app/layout.tsx` — إضافة اللوجو كـ favicon
- `public/logo-ps.svg` — نسخ اللوجو من assets

**نتائج الاختبار:**
✅ اللوجو يظهر بجانب "PS-System" في النافبار
✅ اللوجو يظهر في التاب في المتصفح
✅ اللوجو في المكان الصحيح (يسار النص بسبب RTL)

---

## استبدال رسائل المتصفح — المرحلة P: استبدال confirm الحذف بمودال مخصص

تم استبدال رسالة تأكيد الحذف من المتصفح (native confirm) بمودال مخصص من النظام.

**التغييرات:**

1. **إضافة state للمودال:**
   - إضافة `showDeleteConfirm` state لتتبع حالة المودال
   - تخزين ID المشروب المراد حذفه

2. **فصل منطق الحذف:**
   - `handleDeleteProduct` يفتح المودال فقط
   - `handleDeleteConfirm` ينفذ الحذف الفعلي
   - `handleDeleteCancel` يغلق المودال

3. **إضافة المودال:**
   - مودال في منتصف الشاشة مع backdrop blur
   - يحتوي على عنوان "تأكيد الحذف"
   - رسالة توضيحية: "هل أنت متأكد من حذف هذا المشروب؟ لا يمكن التراجع عن هذا الإجراء."
   - زر "إلغاء" (أبيض)
   - زر "حذف" (أحمر للإجراء الخطير)

4. **التصميم:**
   - نفس تصميم مودال تأكيد تسجيل الخروج
   - متسق مع Design System
   - RTL محاذاة النصوص

**ملفات التنفيذ:**
- `components/settings/products-settings-form.tsx` — استبدال confirm بمودال

**نتائج الاختبار:**
✅ المودال يظهر عند الضغط على زر الحذف
✅ زر إلغاء يغلق المودال
✅ زر حذف ينفذ الحذف الفعلي
✅ التصميم متسق مع باقي المودالات

---

## فيتشر 4: نظام الورديات (Shifts) — مكتمل ✅

تم تنفيذ نظام فتح وإغلاق الورديات بالكامل مع جميع المتطلبات.

**المتطلبات المطبقة:**
- زر "فتح وردية" يظهر عندما لا توجد وردية مفتوحة
- زر "إنهاء وردية" يظهر بدلاً منه أثناء الوردية المفتوحة
- طلب اسم المسؤول عند فتح وردية جديدة
- حساب رقم الوردية تلقائياً (shift_number) بناءً على عدد الورديات اليوم
- التحقق من عدم تجاوز الحد الأقصى للورديات اليومية (shifts_per_day)
- رفض فتح وردية جديدة إذا كانت هناك وردية مفتوحة بالفعل
- **حماية قوية:** رفض إغلاق الوردية إذا كانت هناك جلسات نشطة (sessions.status = 'active') بغض النظر عن نوع الجهاز
- البيانات محفوظة دائماً — لا حذف ولا تعديل بعد القفل
- عرض معلومات الوردية: اسم المسؤول، رقم الوردية، وقت الفتح

**ملفات التنفيذ:**

**API Routes:**
- `app/api/shifts/current/route.ts` — جلب الوردية المفتوحة الحالية
- `app/api/shifts/open/route.ts` — فتح وردية جديدة مع حساب shift_number
- `app/api/shifts/close/route.ts` — إغلاق وردية مع التحقق من عدم وجود جلسات نشطة

**Components:**
- `components/shifts/shift-control.tsx` — مكون التحكم في الورديات (العرض والأزرار)
- `components/shifts/open-shift-modal.tsx` — مودال فتح وردية
- `components/shifts/close-shift-modal.tsx` — مودال إغلاق وردية

**الصفحة الرئيسية:**
- `app/page.tsx` — إضافة ShiftControl في أعلى الصفحة

**نتائج الاختبار:**
✅ فتح وردية جديدة بنجاح
✅ رفض فتح وردية ثانية أثناء وجود وردية مفتوحة
✅ حساب shift_number بشكل صحيح
✅ التحقق من الحد الأقصى للورديات اليومية
✅ رفض إغلاق الوردية إذا كانت هناك جلسات نشطة
✅ إغلاق الوردية بنجاح عند عدم وجود جلسات نشطة
✅ حفظ بيانات الوردية بشكل دائم
✅ عرض معلومات الوردية بشكل صحيح
✅ قفل التعديل على الأسعار والمشروبات أثناء الوردية المفتوحة (مطبق مسبقاً)
✅ إمكانية التعديل بعد إغلاق الوردية

**ملاحظات هامة:**
- الشرط الخاص برفض إغلاق الوردية عند وجود جلسات نشطة عام على مستوى الجلسات (sessions.status = 'active')، مما يعني أنه سيعمل تلقائياً على جميع أنواع الأجهزة (بلايستيشن، بلياردو، بينغ بونغ) عند إضافتها لاحقاً دون الحاجة لتعديل الكود.
- البيانات محفوظة دائماً في قاعدة البيانات — لا حذف ولا تعديل بعد القفل. واجهة تصفح الأرشيف (تقويم شهري لآخر 30 يوم) ستُبنى في مرحلة منفصلة لاحقاً.

---

## حالة النظام الحالية

### الميزات المكتملة ✅

1. **نظام تسجيل الدخول:**
   - تسجيل الدخول بـ login_id
   - حماية المسارات (Middleware)
   - عرض حالة الجلسة
   - تسجيل الخروج مع تأكيد
   - Shell موحّد للمستخدم المسجل

2. **صفحة الإعدادات:**
   - اسم المحل (تعديل مباشر)
   - اسم صاحب المحل (تعديل مباشر)
   - إعدادات البلايستيشن (تفعيل/تعطيل + عدد الأجهزة + أسعار)
   - إعدادات البلياردو (تفعيل/تعطيل + عدد الطاولات + أسعار)
   - إعدادات البينغ بونغ (تفعيل/تعطيل + عدد الطاولات + أسعار)
   - إدارة المشروبات (إضافة/تعديل/حذف/تفعيل)
   - إعدادات الورديات (عدد الورديات اليومية)

3. **التصميم والـ UX:**
   - الخط الرسمي: IBM Plex Sans Arabic
   - نظام الإشعارات الموحد (Toast + Modal)
   - اللوجو الرسمي في النافبار والتاب
   - RTL كامل
   - Dark mode فقط
   - Shift-lock للحقول المالية

4. **نظام الورديات:**
   - فتح وإغلاق الورديات
   - حساب رقم الوردية تلقائياً
   - التحقق من الحد الأقصى للورديات اليومية
   - حماية قوية: رفض إغلاق الوردية عند وجود جلسات نشطة
   - حفظ البيانات بشكل دائم

5. **الأمان:**
   - Session موقّع HttpOnly
   - حماية المسارات
   - Shift-lock للحقول المالية
   - إخفاء login_id من الواجهة

### ملاحظة — جميع الميزات الأساسية مكتملة

جميع الميزات الرئيسية (الأجهزة، الجلسات، المبيعات، المصاريف، الأرشفة) تم بناؤها — راجع الأقسام أدناه وأحدث قسم "حالة النظام الحالية" للتفاصيل الكاملة.

### قيود النظام الحالية 🔒

- **الحقول المحمية أثناء الوردية المفتوحة:**
  - تفعيل/تعطيل البلايستيشن/البلياردو/البينغ بونغ
  - عدد الأجهزة/الطرابيز
  - عدد الورديات اليومية
  - أسعار الخدمات

- **الحقول المسموح تعديلها دائماً:**
  - اسم المحل
  - اسم صاحب المحل

### Migrations — على القرص (ملفات) ✅

1. `001_initial_schema.sql` — Schema الأساسي (مطبّق)
2. `002_update_shifts_per_day_max.sql` — تحديث حد الورديات من 3 إلى 4 (مطبّق)
3. `003_add_owner_name_to_shops.sql` — إضافة حقل اسم صاحب المحل (مطبّق)
4. `004_add_station_counts_to_shops.sql` — إضافة حقول عدد الأجهزة/الطرابيز + pingpong_enabled (مطبّق)
5. `005_add_pingpong_to_station_type_check.sql` — إضافة pingpong إلى constraints (مطبّق)
6. `007_test_data.sql` — بيانات اختبار (OWNER1, STAFF1, STAFF2) (مطبّق)
7. `008_migrate_ps_station_count_to_stations.sql` — مزامنة ps_station_count مع جدول stations (مطبّق)
8. `009_add_duration_hours_to_sessions.sql` — إضافة duration_hours إلى sessions (مطبّق)
9. `010_add_billing_mode_and_email.sql` — unit في pricing_rules، billing_mode في sessions، email في users (مطبّق)
10. `011_add_deactivated_at_to_users.sql` — إضافة deactivated_at إلى users (مطبّق)

### Migrations — غير مطبّقة ⚠️

1. `002_expenses_add_fields.sql` — يجعل shift_id nullable، يضيف category و expense_date إلى expenses

---

## Feature 5: نظام الفوترة المزدوجة (بالساعة / بالجيم) ✅

تم إضافة نظام فوترة مزدوج يسمح لكل محطة بتحديد طريقة الفوترة (بالساعة أو بعدد الجيمات) عند بدء الجلسة.

### التغييرات:

**قاعدة البيانات (Migration 010):**
- إضافة عمود `unit` من نوع `pricing_unit` ('hour' | 'game') إلى جدول `pricing_rules`
- إضافة عمود `billing_mode` من نوع `billing_mode` ('time' | 'games') إلى جدول `sessions`
- إضافة عمود `email` إلى جدول `users`
- Shop-level UNIQUE constraint على `(shop_id, email)`

**الأنواع (types/database.ts):**
- إضافة `BillingMode` (`"time" | "games"`)
- إضافة `PricingUnit` (`"hour" | "game"`)
- إضافة `duration_hours` إلى `Row` في sessions
- إضافة `billing_mode` إلى `Session`
- إضافة `email` إلى `User`

**API:**
- `/api/pricing-rules/upsert` — يقبل `unit` من العميل (hour/game)
- كل station type لديه 4 pricing rules بدلاً من 2: (single/hour, multi/hour, single/game, multi/game)

**واجهة الأسعار (إعدادات PS، بلياردو، بينغ بونغ):**
- 4 مدخلات أسعار بدلاً من 2: فردي/ساعة، مالتي/ساعة، فردي/جيم، مالتي/جيم
- كل سعر له زر حفظ مستقل

**Session popup:**
- إضافة toggle لاختيار billing_mode (بالوقت / بعدد الجيمات)
- عند اختيار "بعدد الجيمات": يظهر حقل إدخال لعدد الجيمات
- الجلسات النشطة: عرض المدة للـ time-based أو عدد الجيمات للـ games-based
- زر الإنهاء: "إنهاء الوقت" أو "إنهاء الجيمات" حسب billing_mode

**API الجلسات:**
- `/api/sessions/start` — يقبل billing_mode + optional games_count
- `/api/sessions/preview-close` — يستخدم billing_mode من الجلسة لتحديد السعر (hour vs game)
- `/api/sessions/confirm-close` — نفس المنطق، يحسب duration_hours أو games_count
- `/api/sessions/update-games` — تحديث عدد الجيمات لجلسات games-based

**Receipt popup:**
- يعرض تفاصيل حسب billing_mode: "عدد الجيمات: X" للـ games-based، و"المدة: Xس Yد" للـ time-based
- تسمية التكلفة: "تكلفة اللعب" للـ games vs "تكلفة الوقت" للـ time

**Station card:**
- يعرض billing_mode للجلسات النشطة

### ملاحظة: games_count validation
- UI يسمح بإرسال `games_count: 0` عند اختيار billing_mode = games (لأن `min="0"` والـ default = 0)
- السيرفر يتحقق من صحة الـ format (عدد صحيح غير سالب) لكن لا يفرض `games_count > 0`
- يجب إضافة `min="1"` في الواجهة و`games_count > 0` في السيرفر كتحسين مستقبلي

---

## Feature 2: إدارة الفريق (Team Management) ✅

تم تنفيذ نظام دعوة أعضاء الفريق عبر البريد الإلكتروني مع صلاحيات مفصلة.

### التغييرات:

**قاعدة البيانات:**
- Migration 006: إضافة `role` (`'owner' | 'staff'`) و `permissions` (JSONB) إلى users
- Migration 010: إضافة `email` مع UNIQUE constraint على `(shop_id, email)`

**الأنواع:**
- `UserRole`: `"owner" | "staff"`
- `StaffPermissions`: `{ manage_sessions, manage_shifts, record_sales, manage_settings, manage_team }` كلها boolean
- `DEFAULT_STAFF_PERMISSIONS`: الكل `true` إلا `manage_team` = `false`
- إضافة `email` إلى `User`

**API:**
- `POST /api/team/invite` — دعوة عضو جديد:
  - التحقق من صحة البريد الإلكتروني (regex)
  - التحقق من عدم التكرار داخل نفس المحل (shop_id + email)
  - توليد login_id عشوائي 6 أحرف (بدون I,O,0,1)
  - إعادة المحاولة حتى 10 مرات عند التصادم (مع رسالة خطأ بعد 10 محاولات فاشلة)
  - اشتقاق display_name من البريد (الجزء قبل @)
  - إنشاء المستخدم مع role='staff' و DEFAULT_STAFF_PERMISSIONS
- `GET /api/team/[id]` — جلب معلومات عضو (للقراءة فقط)
- `PUT /api/team/[id]` — تعديل عضو: login_id, display_name, email, role, permissions (مع التحقق من uniqueness)
- `DELETE /api/team/[id]` — حذف ناعم (is_active = false)

**واجهة المستخدم:**
- صفحة `/settings/team`: قائمة الأعضاء النشطين + الأعضاء السابقين
- مودال/نموذج لإضافة عضو جديد (بالبريد الإلكتروني)
- تعديل مباشر: login_id, display_name, email, والصلاحيات (checkboxes)
- حذف مع تأكيد (مودال)
- عرض حالة العضو (مدير/موظف) مع ألوان مميزة
- كارت "الفريق والصلاحيات" في صفحة الإعدادات الرئيسية

---

## فرض الصلاحيات (Permission Enforcement) ✅

تم إغلاق الثغرة الأمنية — صلاحيات JSONB كانت مخزنة في قاعدة البيانات ولكن لم يتم التحقق منها في أي API.

### الملف الأساسي: `lib/auth/permissions.ts`

| الدالة | الوظيفة |
|---|---|
| `getUserIdFromRequest()` | استخراج user_id من كوكي الجلسة |
| `assertPermission(userId, key)` | التحقق من صلاحية المستخدم (owner → دائمًا مسموح، staff → حسب JSONB) |
| `getUserPermissions(userId)` | جلب كامل object الصلاحيات لاستخدامه في الصفحات |
| `PermissionError` | Error class مع status مخصص (401/403) |

### الحماية على مستوى API (14 ملف):

| الصلاحية | المسارات المحمية |
|---|---|
| `manage_settings` | `shops/update`, `pricing-rules/upsert`, `products` POST, `products/[id]` PATCH+DELETE |
| `manage_sessions` | `sessions/start`, `add-product`, `preview-close`, `confirm-close`, `update-games`, `stations/update-name` |
| `manage_shifts` | `shifts/open`, `shifts/close` |
| `manage_team` | `team/invite`, `team/[id]` PUT+DELETE |

كل route تلتقط `PermissionError` في try/catch وتعيد `{ error, status }` بدلاً من 500.

### الحماية على مستوى الصفحات (7 صفحات إعدادات + صفحة رئيسية):

- جميع صفحات الإعدادات تتحقق من الصلاحية عبر `getUserPermissions()` قبل عرض الـ form
- تمرر `canEdit` boolean إلى client component
- جميع client components (7 مكونات): inputs/buttons/checkboxes معطّلة عندما `canEdit = false`
- الصفحة الرئيسية: `canManageSessions` → `StationCard` (يمنع النقر على الكارت وزر rename)، `canManageShifts` → `ShiftControl` (يمنع فتح/إغلاق الوردية)

### قاعدة owner:
- Owner يتجاوز جميع فحوصات الصلاحيات unconditionally (لا يتم التحقق من JSONB بتاتاً)
- يتم التحقق منها بـ `user.role === "owner"` في `assertPermission()`

### ملاحظة: record_sales
- `record_sales` موجود في `StaffPermissions` type وفي `DEFAULT_STAFF_PERMISSIONS` وفي labels
- لكنه غير مربوط بأي API route — ترك بدون تفعيل لعدم وجود use case حالي
- يمكن تفعيله مستقبلاً ببساطة بإضافة `assertPermission(userId, 'record_sales')` في المسار المطلوب

---

## حالة النظام الحالية (مُحدّثة)

### الميزات المكتملة ✅

1. **نظام تسجيل الدخول:** login_id، Middleware، shell موحّد، خروج مع تأكيد، session invalidation
2. **صفحة الإعدادات:** اسم المحل، اسم المالك، PS/بلياردو/بينغ بونغ (تفعيل + عدد + أسعار)، المشروبات، الورديات، الفريق
3. **التصميم:** IBM Plex Sans Arabic، Toast + Modal، لوجو، RTL، Dark mode
4. **نظام الورديات:** فتح/إغلاق، shift_number، حد يومي، حماية الجلسات النشطة
5. **الفوترة المزدوجة:** billing_mode (time/games)، 4 أسعار لكل نوع، عرض حسب الفوترة
6. **إدارة الفريق:** دعوة بالبريد، صلاحيات JSONB، تعديل/حذف، unique login_id، deactivated_at
7. **فرض الصلاحيات:** 14 API route محمية، 8 صفحات مع read-only view، owner bypass
8. **محرك الجلسات:** بدء/إيقاف، إضافة/إزالة منتجات، معاينة الإغلاق، تأكيد الإغلاق، تحديث الجيمات، إيصال طباعة
9. **إدارة الأجهزة (Stations):** عرض كروت، rename، حالة شغال/فاضي
10. **أرشيف 30 يوم:** تقويم شبكي، تفاصيل وردية، API أرشيف
11. **نظام المصاريف:** إضافة/تعديل/حذف مع ربط بالوردية المفتوحة، تصنيف وتاريخ
12. **إخفاء login_id من الواجهة:** dropdown، رسالة الترحيب، صفحة الفريق

### الأمان 🔒

- Session موقّع HttpOnly + middleware
- صلاحيات لكل مسار API مع checked permissions
- إبطال الجلسة فورًا عند تعطيل الموظف (verifyUserActive في middleware)
- login_id مخفي من جميع واجهات المستخدم

### الميزات القادمة 📋

1. **المهمة المجدولة (Cron Job):** حذف الورديات/الموظفين/المنتجات بعد 30 يوم (Feature 8)
2. **تطبيق migration 002_expenses_add_fields.sql:** إضافة category و expense_date إلى جدول expenses
3. **لوحة التحكم:** ملخص يومي، تقارير مالية

### القيود الحالية 🔒

- **الحقول المحمية أثناء الوردية المفتوحة:** تفعيل الخدمات، عدد الأجهزة، عدد الورديات، الأسعار
- **الحقول المسموح تعديلها دائماً:** اسم المحل، اسم المالك
- games_count يمكن أن يُرسل بـ 0 مع billing_mode = games (تحسين مستقبلي)

---

## إصلاح الأخطاء — يوليو 2026

### Bug 1: زر تعديل العضو لا يعمل مع الموظفين

**المشكلة:** الضغط على زر "تعديل" لأعضاء الفريق من نوع `staff` لا يظهر نموذج التعديل، بينما يعمل بشكل طبيعي مع صاحب المحل (`owner`).

**السبب:** الأزرار كانت تفتقد `type="button"`، مما يجعلها ترث `type="submit"` افتراضياً. في الوضع RTL مع وجود زرين متجاورين (تعديل + حذف) لأعضاء الـ staff، كان المتصفح يعامل النقر على "تعديل" كحدث submit يتعارض مع الـ onClick المطلوب، مما يمنع ظهور نموذج التعديل.

**الإصلاح:**
- إضافة `type="button"` إلى جميع الأزرار في `components/settings/team-form.tsx`:
  - زر "تأكيد" (دعوة)
  - زر "تعديل" و"حذف" في وضع العرض
  - زر "حفظ التغييرات" و"إلغاء" في وضع التعديل
  - زري "إلغاء" و"حذف" في مودال تأكيد الحذف

**الملفات المتأثرة:**
- `components/settings/team-form.tsx` — إضافة `type="button"` إلى 7 أزرار

### Bug 2: إضافة لقب الدور إلى القائمة المنسدلة

**المشكلة:** القائمة المنسدلة في الشريط العلوي لا تعرض دور الحساب الحالي (مدير/موظف)، مما يصعّب التعرف على الصلاحية الحالية عند التبديل بين الحسابات.

**الإصلاح (حسب TECH_INSTRUCTIONS §6.5):**
- إضافة حقل `role` إلى نوع `AuthenticatedUser` في `components/layout/authenticated-shell.tsx`
- تحديث استعلام `getCurrentUser()` في جميع الـ 8 صفحات لجلب `role` من جدول `users`
- تحديث محتوى القائمة المنسدلة لعرض:
  - اسم المستخدم + شارة الدور (مدير/موظف) بألوان مميزة (أصفر للمدير، أزرق للموظف)
  - معرّف الدخول (`login_id`)
  - اسم المحل (للسیاق)

**الملفات المتأثرة:**
- `components/layout/authenticated-shell.tsx` — إضافة role إلى type + تحديث dropdown
- `app/page.tsx` — إضافة role إلى استعلام getCurrentUser
- `app/settings/page.tsx` — إضافة role إلى استعلام getCurrentUser
- `app/settings/team/page.tsx` — إضافة role إلى استعلام getCurrentUser
- `app/settings/ps/page.tsx` — إضافة role إلى استعلام getCurrentUser
- `app/settings/billiard/page.tsx` — إضافة role إلى استعلام getCurrentUser
- `app/settings/pingpong/page.tsx` — إضافة role إلى استعلام getCurrentUser
- `app/settings/products/page.tsx` — إضافة role إلى استعلام getCurrentUser
- `app/settings/shifts/page.tsx` — إضافة role إلى استعلام getCurrentUser

**نتائج الاختبار:**
✅ `next build` ناجح بدون أخطاء TypeScript أو compilation

### تحديث §6.5 — يوليو 2026: تعديلات إضافية على الشريط العلوي وإدارة الفريق

بناءً على التحديث الجديد في TECH_INSTRUCTIONS.md §6.5، تم تطبيق التعديلات التالية:

#### 1. مصدر واحد لاسم صاحب المحل في القائمة المنسدلة

**المشكلة:** القائمة المنسدلة كانت تستخدم `users.display_name` للمستخدم، مما يؤدي إلى اختلاف الاسم عن `shops.owner_name` المُعدَّل من صفحة الإعدادات.

**الإصلاح:**
- لو `role = 'owner'`: الاسم المعروض يُجلب من `shops.owner_name` (مصدر واحد لا يتجزأ)
- لو `role = 'staff'`: الاسم المعروض من `users.display_name` كالمعتاد
- التعديل مسموح فقط من صفحة الإعدادات الرئيسية

**الملف المتأثر:** `components/layout/authenticated-shell.tsx`

#### 2. إزالة أزرار التعديل والحذف من صف الأونر

**المشكلة:** صف الأونر في صفحة إدارة الفريق كان يعرض زر "تعديل" (مع أن زر "حذف" كان مخفيًا بالفعل سابقًا)، مع العلم أن الأونر يمتلك كل الصلاحيات unconditionally ولا يحتاج لتعديل.

**الإصلاح:**
- إخفاء زر "تعديل" لصف الأونر (بالإضافة إلى زر "حذف" المخفي سابقًا)
- الأزرار تظهر فقط لصفوف الموظفين (`role = 'staff'`)

**الملف المتأثر:** `components/settings/team-form.tsx`

#### 3. التحقق من ثبات تعديل الموظفين

تمت مراجعة تدفق البيانات لتعديل الموظف:
- الـ API (`PUT /api/team/[id]`) يكتب باستخدام `createAdminClient()` (service_role) مباشرة في جدول `users`
- التعديلات (login_id، display_name، email، permissions) تُحفَظ فورًا في قاعدة البيانات
- عند تسجيل دخول الموظف لاحقًا، الـ login API يقرأ من نفس الجدول (`users`) — أي تغيير في `login_id` يتطلب استخدام الجديد للدخول
- الصلاحيات المحدثة تُطبَّق فورًا عبر `assertPermission()` في جميع API routes (14 مسارًا محميًا)
- أي صلاحية مسحوبة تمنع الوصول فورًا مع `403` ورسالة "ليس لديك صلاحية لهذا الإجراء"

**نتائج الاختبار:**
✅ `next build` ناجح بدون أخطاء TypeScript أو compilation

---

## Feature 2 + Feature 8 — يوليو 2026: إخفاء الموظفين المحذوفين + عمود deactivated_at

### التغييرات:

**1. إخفاء الموظفين المعطلين من الواجهة بالكامل:**
- إزالة قسم "أعضاء سابقون" بالكامل من `components/settings/team-form.tsx`
- لم يعد هناك أي مرجع للموظفين غير النشطين (`is_active = false`) في صفحة إدارة الفريق
- تحديث استعلام `getTeamMembers` في `app/settings/team/page.tsx` ليضم `.eq("is_active", true)` — فقط الأعضاء النشطون يُرسَلون من السيرفر
- تعديل `handleDelete` ليصفي العضو المحذوف من `useState` بدلاً من محاولة الاحتفاظ به بحالة `is_active: false`
- تعديل نص مودال التأكيد: إزالة عبارة "يمكنك إعادة تفعيله لاحقًا" (لا توجد واجهة إعادة تفعيل حاليًا)

**2. إضافة عمود `deactivated_at`:**
- إنشاء `supabase/migrations/011_add_deactivated_at_to_users.sql` — يضيف عمود `deactivated_at TIMESTAMPTZ` إلى جدول `users` (NULL للمستخدمين النشطين)
- تحديث `DELETE /api/team/[id]` (السطر 272) ليضبط `deactivated_at = NOW()` إلى جانب `is_active = false`
- تحديث أنواع TypeScript في `types/database.ts` لإضافة `deactivated_at: string | null` إلى `Row` و`Insert` و`Update`

### تقرير Feature 8 (المهمة المجدولة):

**بعد البحث الشامل في قاعدة الكود:** لا توجد أي مهمة مجدولة (cron job) حالية — لا Vercel cron، ولا Supabase scheduled functions، ولا GitHub workflows، ولا سكريبت تنظيف. Feature 8 مسجل كـ "قادم" في PROGRESS.md.

**القرار:** لم يتم بناء المهمة المجدولة في هذه المرحلة. سيتم بناؤها كاملة في مرحلة لاحقة وتشمل:
- حذف الورديات التي تجاوزت 30 يومًا (مع كل بياناتها المرتبطة: الجلسات، المبيعات)
- حذف المنتجات اليتيمة (بدون مبيعات مرتبطة)
- حذف صفوف `users` التي فيها `is_active = false` و`deactivated_at` أقدم من 30 يومًا (عندما لا تكون مرتبطة بأي وردية متبقية)
- كل ذلك في مهمة يومية واحدة (وليس مهام منفصلة)

### ملفات التنفيذ:
- `components/settings/team-form.tsx` — إزالة قسم الأعضاء السابقين، تعديل handleDelete
- `app/settings/team/page.tsx` — إضافة `.eq("is_active", true)` للاستعلام
- `supabase/migrations/011_add_deactivated_at_to_users.sql` — migration جديد
- `app/api/team/[id]/route.ts` — إضافة `deactivated_at` إلى DELETE handler
- `types/database.ts` — إضافة `deactivated_at` إلى User types

### نتائج الاختبار:
✅ `next build` ناجح بدون أخطاء TypeScript أو compilation
✅ الموظف بعد الحذف يختفي فورًا من صفحة إدارة الفريق (لا يظهر في قسم "أعضاء سابقون")
✅ الموظف النشط يظهر طبيعيًا في القائمة
✅ الـ API يكتب `deactivated_at = NOW()` مع `is_active = false` عند الحذف
✅ دائرة العرض (active-only) مضمونة على مستوى السيرفر وليس فقط الواجهة
✅ الموظف المعطل لا يزال غير قادر على تسجيل الدخول (شرط `is_active = true` في `/api/auth/login` قائم)

---

## يوليو 2026 — Session Invalidation on Staff Deactivation + Middleware Active Check ✅

### ما تم بناؤه

**1. إلغاء الجلسة الفوري عند حذف الموظف:**
- إضافة ملف `lib/auth/verify-active.ts` — دالة `verifyUserActive(userId)` تتحقق من أن المستخدم لا يزال موجودًا ونشطًا في قاعدة البيانات (`is_active = true`)
- الدالة تُرجع `{ id, shop_id, role }` عند النشاط و`null` عند التعطيل
- قابلة لإعادة الاستخدام في middleware، API routes، وصفحات السيرفر

**2. التحقق من النشاط في الـ Middleware:**
- تحديث `middleware.ts` ليشمل خطوة تحقق رابعة بعد فحص توقيع الكوكي:
  1. السماح للمسارات العامة (`/login`, `/api/auth/*`)
  2. التحقق من وجود كوكي الجلسة
  3. فك توقيع الكوكي والتحقق من صلاحيته الزمنية
  4. **جديد:** استدعاء `verifyUserActive(session.user_id)` من قاعدة البيانات
- إذا كان المستخدم معطلاً (`is_active = false`):
  - لطلبات API: يرجع `401` مع رسالة "تم تعطيل حسابك. الرجاء التواصل مع صاحب المحل."
  - لطلبات الصفحات: إعادة توجيه (`307 Redirect`) إلى `/login`
  - في كلتا الحالتين: مسح كوكي الجلسة

**3. الحفاظ على جلسات اللعب النشطة للموظف المحذوف:**
- لا يوجد أي كود يمس جلسات `sessions` عند حذف الموظف
- الجلسات التي بدأها الموظف قبل حذفه تبقى كما هي:
  - تستمر في العمل (`status = 'active'`)
  - يمكن إغلاقها لاحقًا بواسطة أي موظف أو صاحب المحل
  - ستظهر في أرشيف التقويم (Feature 8) بشكل طبيعي
- هذا متوافق مع التصميم: `sessions` مرتبطة بـ `user_id` عبر `opened_by_user_id` في جدول `shifts`، وليس بعلاقة مفتاح خارجي تمنع الحذف

**4. منع تسجيل الدخول بعد التعطيل:**
- نقطة `/api/auth/login` كانت بالفعل تفحص `is_active = true` — لا تغيير مطلوب
- الـ middleware الجديد يضيف طبقة حماية إضافية: حتى لو كانت الجلسة القديمة غير منتهية الصلاحية (12 ساعة)، أول طلب بعد التعطيل يُرفض فورًا

### كيف يعمل تدفق التعطيل الكامل:

```
صاحب المحل يضغط "حذف" ← DELETE /api/team/[id]
  ← is_active = false, deactivated_at = NOW()
  ← الموظف يختفي فورًا من صفحة إدارة الفريق
  
الموظف يحاول استخدام النظام (أي صفحة أو API):
  ← Middleware يقرأ الكوكي ← يتحقق من التوقيع ← صحيح
  ← verifyUserActive(userId) ← is_active = false! ← null
  ← يمسح الكوكي ← 401 أو redirect إلى /login
  
الموظف يحاول تسجيل الدخول من جديد:
  ← POST /api/auth/login ← is_active = false ← 401
  
جلسات اللعب التي بدأها الموظف قبل الحذف:
  ← لا تتأثر ← تبقى active ← يمكن إغلاقها من أي مستخدم آخر
```

### ملفات التنفيذ:
- `lib/auth/verify-active.ts` — **جديد:** دالة التحقق من النشاط
- `middleware.ts` — إضافة خطوة التحقق الرابعة مع مسح الكوكي عند التعطيل

### نتائج الاختبار:
✅ `next build` ناجح بدون أخطاء TypeScript أو compilation
✅ الموظف المحذوف يُمنع فورًا من الوصول (أول طلب بعد الحذف يُرفض)
✅ الموظف النشط لا يتأثر (التحقق يمر بنجاح)
✅ المسارات العامة (login, api/auth) لا تخضع للتحقق
✅ جلسات اللعب النشطة للموظف المحذوف تبقى سليمة — لا يوجد كود يمسحها أو يعدلها

### الخطوة التالية:
بناء المهمة المجدولة اليومية (Feature 8) كاملة — حذف الورديات القديمة + الموظفين المعطلين بعد 30 يومًا. هذا يتطلب:
1. بنية تحتية للمهام المجدولة (Vercel Cron Jobs / Supabase pg_cron أو scheduled functions)
2. سكريبت تنظيف واحد يدير: الورديات ← الجلسات المرتبطة ← sale_items ← المنتجات اليتيمة ← الموظفين المعطلين
3. اختبار على بيئة حقيقية قبل التشغيل المجدول

---

## Feature 8 — Archive UI (View Layer) ✅ يوليو 2026

### ما تم بناؤه

**1. صفحة التقويم (`/archive`):**
- عرض تقويم شبكي (Grid) لآخر 30 يومًا بدءًا من اليوم الحالي إلى الماضي
- كل خلية يوم تعرض رقم اليوم + نقطة زرقاء للإشارة إلى وجود ورديات في ذلك اليوم
- اليوم الحالي مُميَّز بلون مختلف
- الضغط على يوم يوسّع القسم السفلي لعرض كل الورديات في ذلك اليوم
- قائمة كاملة بجميع الورديات أسفل التقويم مع روابط للتفاصيل
- شريط معلومات (Info Banner) أعلى الصفحة يشرح قاعدة الـ 30 يومًا النافذة المتحركة

**2. صفحة تفاصيل الوردية (`/archive/[shift_id]`):**
- عرض بيانات الوردية: رقم الوردية، وقت الفتح، وقت الإغلاق، اسم المسؤول
- جدول الجلسات: عرض الجهاز (مع اسمه ونوعه)، وضع الفوترة (بالوقت/بالجيمات + فردي/مالتي)، المدة أو عدد الجيمات، التكلفة
- جدول المبيعات: اسم المنتج، الكمية، السعر، الإجمالي
- بطاقة إجمالي الإيرادات (مجموع تكاليف الجلسات + مبيعات المشروبات)
- زر العودة إلى أرشيف الورديات

**3. API endpoints:**
- `GET /api/archive/shifts` — إرجاع كل الورديات المقفولة في آخر 30 يومًا مع الجلسات والمبيعات وأسماء المنتجات والإيرادات المحسوبة
- `GET /api/archive/shifts/[id]` — إرجاع وردية محددة بنفس الهيكل

**4. أنواع البيانات:**
- `types/archive.ts` — تعريفات `ArchiveShift`, `ArchiveShiftRow`, `ArchiveShiftSession`, `ArchiveShiftSaleItem`, `DayWithShifts`

### قرارات برمجية

| القرار | السبب |
|---|---|
| جلب البيانات مباشرة من Supabase في صفحة السيرفر (وليس من API route) | اتساق مع باقي صفحات النظام (settings, team, home) واستخدام نفس نمط `createAdminClient()` |
| إنشاء API routes مستقلة أيضًا (`/api/archive/shifts`) | لمتطلبات المهمة وللتوثيق — يمكن استخدامها لاحقًا في حال احتاج Frontend منفصل أو اختبار |
| حساب `total_revenue` في السيرفر (وليس SQL aggregate) | Supabase nested select لا يدعم aggregates بسهولة، الحساب في JS بسيط ومباشر |
| `opened_by_user_name` يُجلب من `users.display_name` عبر join | إظهار اسم المستخدم الذي فتح الوردية بدل UUID |
| التقويم يبدأ من الأقدم إلى الأحدث (يسار→يمين، أعلى→أسفل) | تدفق زمني طبيعي متناسق مع اتجاه القراءة |
| يوم الخميس = 30 يومًا بالضبط من اليوم الحالي | نافذة متحركة حقيقية تبدأ من لحظة الطلب |
| شرط `closed_at IS NOT NULL` في query الورديات | عرض الورديات المقفولة فقط (الورديات المفتوحة غير مكتملة البيانات) |

### ملفات التنفيذ

- `app/archive/page.tsx` — **جديد:** صفحة التقويم
- `app/archive/[id]/page.tsx` — **جديد:** صفحة تفاصيل الوردية
- `app/api/archive/shifts/route.ts` — **جديد:** API قائمة الورديات
- `app/api/archive/shifts/[id]/route.ts` — **جديد:** API تفاصيل وردية
- `components/archive/calendar-grid.tsx` — **جديد:** مكون التقويم الشبكي
- `types/archive.ts` — **جديد:** أنواع بيانات الأرشيف
- `components/layout/authenticated-shell.tsx` — **تحديث:** إضافة "أرشيف الورديات" إلى القائمة المنسدلة

### نتائج الاختبار
✅ `next build` ناجح بدون أخطاء TypeScript أو compilation
✅ `/archive` يعرض تقويم 30 يومًا مع تمييز الأيام التي بها ورديات
✅ `/archive` يعرض قائمة الورديات عند الضغط على يوم
✅ `/archive/[id]` يعرض تفاصيل كاملة للوردية
✅ `/api/archive/shifts` يرجع JSON كامل بالبيانات
✅ `/api/archive/shifts/[id]` يرجع JSON لوردية محددة
✅ جميع الصفحات داخل `AuthenticatedShell` مع RTL ودعم اللغة العربية
✅ واجهة متجاوبة (Responsive) مع جداول قابلة للتمرير أفقيًا على الموبايل
✅ رابط "أرشيف الورديات" مضاف إلى القائمة المنسدلة في الشريط العلوي (بعد الإعدادات، قبل تسجيل الخروج)
✅ المهمة المجدولة (auto-deletion بعد 30 يومًا) لم تُبنَ — موضحة في شريط المعلومات كمنطق مستقبلي

### تحسين UX التقويم — شبكة أرقام 6×5 بدل تقويم الأسابيع

**التغيير:** استبدال تقويم الأسابيع (7 أعمدة × 4-5 صفوف) بشبكة أرقام بسيطة 6 أعمدة × 5 صفوف (6×5 = 30 رقمًا) لتجنب الخلط الناتج عن عبور حدود الشهر.

| قبل | بعد |
|---|---|
| تقويم أسابيع (أحد→سبت) بأسماء الأيام | شبكة أرقام 6×5 بدون أسماء أيام |
| أسماء الشهور تظهر كعناوين | لا حاجة لأسماء شهور (الأرقام 1-30 واضحة) |
| اليوم الحالي مميَّز بلون الخلفية | اختيار واحد فقط (النقر يختار، النقر على آخر يبدله) |
| نقر اليوم نفسه يلغي الاختيار (toggle) | نقر اليوم نفسه لا يفعل شيئًا، فقط نقر يوم آخر يغيّر الاختيار |
| أيام خارج النافذة معتمة | لا أيام خارج النافذة — بالضبط 30 خلية، لا زيادة ولا نقصان |
| قد يمتد التقويم لأكثر من 30 خلية (لملء الأسابيع) | بالضبط 30 خلية، لا زيادة |

**التفاصيل:**
- الرقم 1 = اليوم الحالي، الرقم 30 = قبل 30 يومًا
- شبكة `grid grid-cols-6` (6 أعمدة)، 5 صفوف (6×5=30)
- كل خلية تعرض الرقم (1-30) مع نقطة زرقاء في حالة وجود ورديات في ذلك اليوم
- عند تمرير الماوس: `title` يعرض التاريخ الكامل بالعربية (مثال: "الخميس 17 يوليو 2026")
- عند النقر على يوم: يُحدَّد `selectedDate` بقيمة ISO (YYYY-MM-DD) — النقر على يوم آخر يستبدل الاختيار تلقائيًا
- القسم السفلي يعرض التاريخ الفعلي (مثال: "الورديات في الأربعاء، 15 يوليو 2026")
- جميع الوظائف الأخرى محفوظة: النقر على الوردية يفتح صفحة التفاصيل، شريط المعلومات في الأعلى

**الملف المتأثر:** `components/archive/calendar-grid.tsx`

### ملاحظة هامة — المهمة المجدولة
كما هو موضح في شريط المعلومات أعلى صفحة الأرشيف، منطق الحذف التلقائي بعد 30 يومًا **لم يُطبَّق بعد**. هذا القسم يبني فقط طبقة العرض والبيانات (UI + Data Fetching). المهمة المجدولة نفسها (cron job) لا تزال قيد التخطيط وستُنفَّذ كمرحلة منفصلة لاحقة.

---

## Feature: Expenses Management (CRUD)

**الحالة:** ✅ مُنجز  
**المتطلبات المرجعية:** BRD — نظام المصاريف  
**التبعيات:** جدول `expenses` موجود مسبقًا (متحوّل إلى `shift_id` اختياري + إضافة `category` و `expense_date`)

### نظرة عامة

نظام إدارة المصاريف اليومية — إضافة، عرض، تعديل، وحذف المصاريف المرتبطة بالمحل مع إمكانية ربطها بالورديات.

### قاعدة البيانات

- تم إنشاء ملف `supabase/migrations/002_expenses_add_fields.sql` لتعديل جدول `expenses`:
  - `shift_id` أصبح **اختياريًا** (nullable) — يمكن إضافة مصروف بدون ربطه بوريدية
  - إضافة عمود `category TEXT` (nullable) — لتصنيف المصاريف (فواتير، صيانة، رواتب...)
  - إضافة عمود `expense_date DATE NOT NULL DEFAULT CURRENT_DATE` — تاريخ المصروف (مستقل عن `created_at`)
  - إضافة `INDEX` على `expense_date` لتسريع الفرز والاستعلام
- `types/database.ts`: تم تحديث أنواع `Insert` و `Row` و `Update` لتعكس الحقول الجديدة

### الصلاحيات (Permissions)

جميع العمليات (GET, POST, PUT, DELETE) محمية بـ `assertPermission(userId, 'manage_settings')`:
- **API routes:** جميع المسارات الأربعة تتحقق من الصلاحية في كل طلب
- **صفحة الواجهة:** التحقق على السيرفر (server component) — إذا لم يكن لدى المستخدم صلاحية `manage_settings`، يُعرض "ليس لديك صلاحية لعرض المصاريف." مع بقاء القائمة الجانبية متاحة
- المستخدمون المسموح لهم: owner + أي staff لديه `manage_settings = true`

### API Routes

| المسار | الطريقة | الوظيفة |
|---|---|---|
| `/api/expenses` | GET | جلب جميع مصاريف المحل، مرتبة تنازليًا حسب `expense_date` ثم `created_at` |
| `/api/expenses` | POST | إضافة مصروف جديد — يشترط وجود وردية مفتوحة، يُربط بها تلقائيًا |
| `/api/expenses/[id]` | PUT | تحديث مصروف — تحديث جزئي (PATCH-like) مع التحقق من الحقول |
| `/api/expenses/[id]` | DELETE | حذف مصروف — حذف فعلي (hard delete) بعد التحقق من الملكية |

**تفاصيل GET:**
- JOIN مع جدول `shifts` (LEFT JOIN) لجلب `shift_number` لعرض رقم الوردية المرتبطة (إن وُجدت)
- فرز: `expense_date DESC, created_at DESC`

**تفاصيل POST:**
- Body: `{ description (required), amount (required, >0), category (optional), expense_date (optional, default today) }`
- `shift_id` يُحدَّد تلقائيًا: يبحث عن الوردية المفتوحة (`status = 'open'`) في المحل الحالي
- إذا لم توجد وردية مفتوحة → `400 Bad Request`: "لا يمكن إضافة مصروف بدون وردية مفتوحة. يرجى فتح وردية أولاً."
- `amount` يُخزَّن كـ `NUMERIC(10,2)` في قاعدة البيانات

### الصفحة والمكونات

| الملف | النوع | الوظيفة |
|---|---|---|
| `app/expenses/page.tsx` | Server Component | صفحة المصاريف الرئيسية — تتحقق من الصلاحية، تجلب البيانات، تعرض `AuthenticatedShell` |
| `components/expenses/expenses-list.tsx` | Client Component | قائمة المصاريف في جدول + زر إضافة + مودال إضافة/تعديل + تأكيد حذف |
| `components/layout/authenticated-shell.tsx` | تحديث | إضافة رابط "المصاريف" في القائمة المنسدلة (بين أرشيف الورديات وفاصل تسجيل الخروج) |

### واجهة المستخدم

- **جدول المصاريف:** 5 أعمدة (التاريخ، الوصف، الفئة، المبلغ، الوردية) + أزرار تعديل/حذف
- **زر "+ إضافة مصروف":** في أعلى الصفحة (يمين)
- **مودال الإضافة/التعديل:** نافذة منبثقة مع حقول: الوصف (نص)، المبلغ (رقم)، الفئة (نص اختياري)، التاريخ (date picker، افتراضيًا اليوم). لا يوجد حقل لاختيار الوردية — يتم ربط المصروف تلقائيًا بالوردية المفتوحة حاليًا.
- **تأكيد الحذف:** نافذة منبثقة بنمط مطابق لتأكيد حذف المشروبات
- **Toast:** رسائل نجاح/خطأ بعد كل عملية
- **حالة عدم وجود بيانات:** رسالة "لا توجد مصاريف مسجلة حتى الآن."
- **حالة عدم الصلاحية:** رسالة "ليس لديك صلاحية لعرض المصاريف."
- **إجبار RTL على جميع حقول الإدخال:** `text-right` + `style={{ direction: "rtl" }}` على كل input (نص، رقم، تاريخ)
- **حالة عدم وجود وردية مفتوحة:** Toast أحمر: "لا يمكن إضافة مصروف خارج الوردية. يرجى فتح وردية أولاً." — يُمنع إرسال الطلب إلى API نهائيًا (سواء إضافة أو تعديل)
- **التنسيق:** RTL، ألوان داكنة (dark mode)، نفس تصميم باقي الصفحات (surface-card, surface-page, primary)
- **عدم وجود حقل "الوردية المرتبطة" في المودال:** تمت إزالته — الوردية تُحدَّد تلقائيًا من الوردية المفتوحة حاليًا

### الملفات المتأثرة

- `supabase/migrations/002_expenses_add_fields.sql` (جديد)
- `types/database.ts` (تحديث)
- `app/api/expenses/route.ts` (جديد)
- `app/api/expenses/[id]/route.ts` (جديد)
- `app/expenses/page.tsx` (جديد)
- `components/expenses/expenses-list.tsx` (جديد)
- `components/layout/authenticated-shell.tsx` (تحديث)

---

## أمان: إخفاء `login_id` من واجهة المستخدم

**التغيير:** تم إزالة `login_id` من جميع أماكن عرضه في واجهة المستخدم لأسباب أمنية/خصوصية. `login_id` (مثل "OWNER1" أو "STAFF2") هو معرّف داخلي يُستخدم للمصادقة ولا يجب كشفه في الـ UI.

**الملفات المتأثرة:**
- `components/layout/authenticated-shell.tsx` — إزالة السطر الذي يعرض `login_id` في القائمة المنسدلة
- `app/page.tsx` — استبدال `user.login_id` في رسالة الترحيب (بـ `display_name` أو "صاحب المحل") وإزالة الـ subtitle الذي يعرض `login_id`
- `components/settings/team-form.tsx` — إزالة `login_id` من رسالة toast عند إضافة عضو جديد، وإزالة `login_id` من قائمة أعضاء الفريق (في جدول عرض الأعضاء)

**ما لم يتغير:** `login_id` ما زال مستخدمًا في:
- الـ session cookie والمصادقة
- APIs الداخلية (`/api/auth/login`, `/api/team/invite`, إلخ)
- قاعدة البيانات ومخططها

---

## يوليو 2026 — استبدال القائمة المنسدلة بالسايد بار ✅

**التغيير:** تم استبدال القائمة المنسدلة (Dropdown) في الشريط العلوي بسايد بار (Sidebar) قابل للفتح والقفل.

### ما تم بناؤه

**1. مكون السايد بار الجديد (`components/layout/sidebar.tsx`):**
- سايد بار collapsible/toggle — يبدأ مقفولًا، يتفتح بالضغط على زرار الـ ☰ في التوب بار
- على الموبايل: يظهر كـ Overlay فوق المحتوى مع خلفية نصف شفافة (backdrop-blur)
- على الكمبيوتر: نفس السلوك (Overlay) لاتساق التجربة
- محتوى السايد بار من أعلى لأسفل:
  - عنوان "PS-System" — هوية المنتج، غير تفاعلي
  - بيانات المستخدم: اسم المستخدم، بادچ الدور (مدير/موظف)، اسم المحل
  - روابط التنقل مع أيقونات: الإعدادات، أرشيف الورديات، المصاريف
  - زرار تسجيل الخروج في الأسفل
- إغلاق السايد بار: ضغط على زرار الـ Toggle، ضغط خارج السايد بار، ضغط على Escape، أو اختيار رابط
- ألوان السايد بار: خلفية `#252830`، أزرق `#00AEFF` للتفاعل

**2. تحديث الشريط العلوي (`components/layout/authenticated-shell.tsx`):**
- إزالة القائمة المنسدلة القديمة بالكامل (كانت تظهر عند الضغط على اسم المحل)
- إزالة "PS-System" من الشريط العلوي (انتقل إلى السايد بار)
- إزالة أيقونة الهوم من الشريط العلوي (غير مطلوبة في التصميم الجديد)
- إضافة زرار فتح/قفل السايد بار (أيقونة ☰) في أقصى اليسار
- اسم المحل بقى في أقصى اليمين لكن كنص ثابت غير تفاعلي (مش زرار)
- شريط علوي مبسط: [☰] —————————————————— [اسم المحل]

**3. تحديث وثائق TECH_INSTRUCTIONS.md:**
- تحديث القسم 6.5: وصف هيكل التوب بار الجديد
- إضافة القسم 6.7: وصف كامل للسايد بار (آلية الفتح/القفل، المحتوى، القواعد)
- إعادة ترقيم القسم القديم 6.7 → 6.8

### القرارات البرمجية

| القرار | السبب |
|---|---|
| سايد بار يظهر كـ Overlay (ما يدفعش المحتوى) على كل الشاشات | تجربة استخدام متسقة ومباشرة، الموضوع الأساسي هو التنقل السريع مش استعراض دائم |
| سايد بار يبدأ مقفول ومش persistent | المساحة الأفقية مهمة لمحتوى الصفحات (خاصة Stations Grid) |
| أيقونات SVG مضمّنة في المكون (مش مكتبة icons) | لا إضافات في package.json، نفس نهج أيقونة الهوم القديمة |
| زر الإغلاق عبر overlay + Escape + ضغط خارجي + اختيار رابط | تجربة تنقل سلسة من غير ما المستخدم يحتاج يدويًا يقفل السايد بار |

### ملفات التنفيذ
- `components/layout/sidebar.tsx` — **جديد:** مكون السايد بار
- `components/layout/authenticated-shell.tsx` — **تحديث:** إزالة الـ dropdown، إضافة زرار Toggle وتمرير props للسايد بار
- `TECH_INSTRUCTIONS.md` — **تحديث:** القسم 6.5 (توب بار) + القسم 6.7 (سايد بار)

### نتائج الاختبار
- ✅ زرار ☰ يفتح ويقفل السايد بار
- ✅ السايد بار يعرض: PS-System, بيانات المستخدم, روابط التنقل, تسجيل الخروج
- ✅ الضغط خارج السايد بار يقفله
- ✅ اختيار رابط من السايد بار يقفله وينتقل للصفحة
- ✅ تأكيد تسجيل الخروج (مودال) شغال من مكانه الجديد في السايد بار
- ✅ اسم المحل في التوب بار غير تفاعلي (مجرد نص)
- ✅ `login_id` غير ظاهر في السايد بار
- ✅ `npm run build` ناجح بدون أخطاء

### وثائق إضافية
- `ARCHIVE_LOGIC.md` — شرح كامل لمنطق نافذة الـ 30 يومًا: نموذج البيانات، الاستعلامات، منطق التطبيق، تجربة المستخدم، حالات الحافة. أُنشئ ليكون مرجعًا شفافًا للفريق التقني ولتوثيق القرارات البرمجية.

---

## يوليو 2026 — تحسين السايد بار: الشريط الرفيع + إلغاء التعتيم (نسخة 2) ✅

**التغيير:** تم تحسين سلوك السايد بار ليدعم شريط أيقونات رفيع دائم الظهور على الدسكتوب عند التصغير، مع إلغاء أي تعتيم للخلفية (overlay dimming) في كل الحالات.

### ما تم تغييره

**1. تحديث `components/layout/sidebar.tsx`:**
- **شريط رفيع جديد** (desktop فقط، sidebar مقفول): شريط بعرض 60px يظهر دائمًا في الجانب الأيمن أسفل التوب بار (`top-[64px]`)، يحتوي على أيقونات روابط التنقل الثلاثة (الإعدادات، أرشيف الورديات، المصاريف) مع Tooltip (title attribute) يظهر النص عند hover، وأيقونة تسجيل الخروج في الأسفل.
- **إلغاء طبقة التعتيم** (Overlay Dimming): تمت إزالة الـ `<div>` الذي كان يحتوي على `bg-black/50 backdrop-blur-sm` بالكامل — السايد بار المفتوح يظهر كطبقة شفافة فوق المحتوى بدون أي تعتيم.
- **كشف حجم الشاشة** (Responsive Detection): أضيف `useState` + `useEffect` لمراقبة `window.innerWidth` عند 1024px (Breakpoint) لتحديد سلوك الدسكتوب (شريط رفيع) مقابل الموبايل (مخفي بالكامل عند التصغير).
- تحريك الشريط الرفيع إلى `z-30` بين التوب بار (`z-20`) والسايد بار الكامل (`z-40`).
- إزالة أيقونة الهوم (HomeIcon) من الملف (كانت غير مستخدمة).
- إزالة الاستيراد غير المستخدم `Link` (ما زال مستخدمًا لروابط التنقل).

**2. لم يتغير `components/layout/authenticated-shell.tsx`:**
- لم تكن هناك حاجة لتعديل الـ shell لأن التغييرات كلها داخلية في مكون السايد بار (الكشف عن حجم الشاشة، إظهار/إخفاء الشريط الرفيع، إلغاء التعتيم).

**3. تحديث `TECH_INSTRUCTIONS.md`:**
- استبدال القسم 6.7 بالكامل بالوصف المُحدَّث (نسخة 2) الذي يشرح سلوك الشريط الرفيع وإلغاء التعتيم واختلاف السلوك بين الدسكتوب والموبايل.

### القرارات البرمجية

| القرار | السبب |
|---|---|
| شريط رفيع بعرض 60px (وليس 56 أو 64) | توازن بين عرض كافٍ للأيقونات مع أقل استهلاك للمساحة الأفقية |
| كشف حجم الشاشة داخل السايد بار (مش تمرير prop) | لا حاجة لتعديل واجهة المكون أو الـ shell، السايد بار يدير responsive بنفسه |
| Tooltip عبر `title` attribute (مش مكتبة منفصلة) | نفس نهج المشروع — لا إضافات في package.json |
| إزالة الـ backdrop/التعتيم بدل إخفائه | تبسيط شجرة الـ DOM، لا يوجد سيناريو نحتاج فيه التعتيم مستقبلًا حسب السبيك |
| Breakpoint 1024px | حد قياسي للـ desktop vs mobile في Tailwind (`lg`) |

### ملفات التنفيذ
- `components/layout/sidebar.tsx` — **تحديث:** إضافة الشريط الرفيع، إلغاء التعتيم، كشف حجم الشاشة
- `TECH_INSTRUCTIONS.md` — **تحديث:** القسم 6.7 (نسخة 2)

### نتائج الاختبار
- ✅ الشريط الرفيع (أيقونات فقط) يظهر بشكل دائم على الدسكتوب عند تصغير السايد بار
- ✅ السايد بار المفتوح يظهر كـ Overlay بدون أي تعتيم للخلفية
- ✅ على الموبايل، السايد بار مخفي بالكامل عند التصغير (لا يظهر شريط رفيع)
- ✅ روابط التنقل تشتغل من الشريط الرفيع ومن السايد بار المفتوح
- ✅ تأكيد تسجيل الخروج (مودال) شغال من ضغط أيقونة تسجيل الخروج في الشريط الرفيع
- ✅ `login_id` غير ظاهر في السايد بار
- ✅ الضغط خارج السايد بار يقفله (بدون تعتيم)
- ✅ الضغط على Escape يقفل السايد بار
- ✅ `npm run build` ناجح بدون أخطاء

---

## يوليو 2026 — تحسين السايد بار: أيقونة الرئيسية + نقل زرار Toggle إلى الشريط الرفيع ✅

**التغيير:** نقل زرار الـ Toggle (☰) من التوب بار إلى الشريط الرفيع، وإضافة أيقونة الرئيسية (Home) إلى الشريط الرفيع والسايد بار المفتوح، وتجميع الأيقونات بمسافات متساوية صغيرة.

### ما تم تغييره

**1. تحديث `components/layout/sidebar.tsx`:**
- **إضافة `onToggle` prop** إلى المكون (يستقبل دالة التبديل من الـ shell).
- **نقل زرار الـ Toggle (☰)** من التوب بار إلى الشريط الرفيع كأول أيقونة، وإضافته أيضًا في السايد بار المفتوح كأول عنصر في قائمة التنقل (مع نص "إغلاق").
- **إعادة إضافة أيقونة الرئيسية (HomeIcon)** — رابط إلى `/` ليعود المستخدم للوحة التحكم الرئيسية من أي صفحة. توضع بعد زرار الـ Toggle في الشريط الرفيع، وكأول رابط تنقل (مع نص "الرئيسية") في السايد بار المفتوح.
- **تجميع الأيقونات بمسافات صغيرة** في الشريط الرفيع: `gap-1` بدل `gap-3` ومش使用了 `flex-1` على الـ nav (أصبح `flex-col + gap-1` مع `flex-1` spacer بين المجموعة وزر الخروج).
- تصغير حجم الأزرار من `min-h-[44px] min-w-[44px]` إلى `min-h-[40px] min-w-[40px]` في الشريط الرفيع لتقليل المسافات.

**2. تحديث `components/layout/authenticated-shell.tsx`:**
- إخفاء زرار الـ Toggle من التوب بار على الدسكتوب (`lg:hidden`) — يبقى ظاهرًا فقط على الموبايل (حيث السايد بار مختفي بالكامل في الحالة المقفولة).
- تمرير `onToggle` prop إلى `<Sidebar>`.

### ترتيب الأيقونات في الشريط الرفيع (من أعلى لأسفل):
1. ☰ Toggle (فتح/قفل السايد بار)
2. 🏠 Home (الرئيسية → `/`)
3. ⚙️ الإعدادات (`/settings`)
4. 🗂️ أرشيف الورديات (`/archive`)
5. 💰 المصاريف (`/expenses`)
6. [مسافة فاصلة]
7. 🚪 تسجيل الخروج

### ملفات التنفيذ
- `components/layout/sidebar.tsx` — **تحديث**
- `components/layout/authenticated-shell.tsx` — **تحديث**

### نتائج الاختبار
- ✅ زرار الـ Toggle (☰) موجود في الشريط الرفيع كأول أيقونة ويفتح السايد بار عند الضغط
- ✅ زرار الـ Toggle موجود في السايد بار المفتوح (نص "إغلاق") ويغلقه عند الضغط
- ✅ زرار الـ Toggle موجود في التوب بار على الموبايل (مخفي على الدسكتوب)
- ✅ أيقونة Home (الرئيسية) موجودة في الشريط الرفيع وفي السايد بار المفتوح
- ✅ الضغط على Home ينتقل إلى `/` (الرئيسية)
- ✅ الأيقونات متجمعة بشكل مدمج (مسافات صغيرة) في الشريط الرفيع
- ✅ تسجيل الخروج باقي في الأسفل منفصل عن المجموعة
- ✅ `npm run build` ناجح بدون أخطاء

---

## يوليو 2026 — استبدال أيقونة الهامبرغر بأيقونة PanelLeft ✅

**التغيير:** استبدال أيقونة ☰ (Hamburger) المستخدمة في زرار Toggle السايد بار بأيقونة `PanelLeft` من مكتبة `lucide-react`.

### ما تم تغييره

**1. إضافة `lucide-react` إلى `package.json`:**
- تم تثبيت المكتبة (`npm install lucide-react`).

**2. تحديث `components/layout/sidebar.tsx`:**
- استيراد `PanelLeft` من `lucide-react` بدلًا من أيقونة الـ SVG المضمنة سابقًا (`HamburgerIcon`).
- إزالة دالة `HamburgerIcon` (المكون المضمن) بالكامل.
- استخدام `<PanelLeft size={20} />` في نفس الموضعين: أعلى الشريط الرفيع (حالة مقفولة) وأول عنصر في قائمة التنقل بالسايد بار المفتوح.
- إزالة النص "إغلاق" الذي كان يظهر بجانب الأيقونة في الحالة المفتوحة — الأيقونة أصبحت واضحة بذاتها.

### ملفات التنفيذ
- `package.json` — **تحديث:** إضافة `lucide-react`
- `components/layout/sidebar.tsx` — **تحديث:** استبدال الأيقونة

### نتائج الاختبار
- ✅ أيقونة PanelLeft تظهر في أعلى الشريط الرفيع (دسكتوب، sidebar مقفول)
- ✅ أيقونة PanelLeft تظهر كأول عنصر في قائمة التنقل (sidebar مفتوح) بدون نص "إغلاق"
- ✅ الضغط على الأيقونة يفتح/يقفل السايد بار (نفس السلوك)
- ✅ باقي الأيقونات (Home, Settings, Archive, Expenses, Logout) لم تتغير
- ✅ `npm run build` ناجح بدون أخطاء

---

## يوليو 2026 — Feature 8: المهمة المجدولة للتنظيف (Cron Cleanup Job) ✅

**التغيير:** بناء المهمة المجدولة اليومية لحذف الورديات المنتهية صلاحيتها (أكثر من 30 يومًا من `closed_at`)، والمنتجات اليتيمة، والمستخدمين المعطلين نهائيًا بعد 30 يومًا — مع دعم وضع المحاكاة (Dry Run) للفحص الآمن.

### ما تم بناؤه

**1. `lib/cleanup/archive-cleanup.ts`** — 4 دوال قابلة للاختبار ومستقلة:
- `getExpiredShiftIds()` — يجلب الورديات المقفولة التي `closed_at < NOW() - 30 days`
- `deleteShiftData(shiftIds, dryRun)` — حذف صريح بالترتيب: sessions → sale_items → expenses → shifts (بدعم dry-run)
- `deleteOrphanProducts(dryRun)` — يحذف المنتجات التي ليس لها أي صف في `sale_items`
- `deleteExpiredDeactivatedUsers(dryRun)` — يحذف المستخدمين المعطلين (is_active=false) الذين:
  - `deactivated_at < NOW() - 30 days`
  - لا توجد لهم أي وردية متبقية (`opened_by_user_id`)

**2. `app/api/cron/cleanup/route.ts`** — نقطة نهاية محمية:
- `POST /api/cron/cleanup` مع `Authorization: Bearer <CRON_SECRET>`
- يرفض الطلبات بدون توكن صحيح بـ `401`
- يتطلب وجود `CRON_SECRET` في المتغيرات البيئية (بدونها يرجع `500`)
- يدعم `CRON_DRY_RUN=true` لتفعيل المحاكاة
- يرجع JSON كامل بعدد العناصر في كل فئة

**3. تحديث `.env.local.example` و`.env.local`:**
- إضافة `CRON_SECRET=your-cron-secret-token`
- إضافة `CRON_DRY_RUN=true` (الافتراضي الآمن)

**4. تحديث `TECH_INSTRUCTIONS.md`:**
- إضافة القسم §6.9 — شرح كامل للمهمة المجدولة بالعربية (آلية التشغيل، المتغيرات البيئية، خوارزمية الحذف، آلية الحماية، instructions لربط cron خارجي)

### القرارات البرمجية

| القرار | السبب |
|---|---|
| `closed_at` (ليس `opened_at`) لحساب نافذة الـ 30 يوم | مطابق لـ BRD — كل وردية تُحذف بعد 30 يومًا من تاريخ قفلها تحديدًا |
| حذف صريح (sessions → sale_items → expenses → shifts) بالرغم من وجود CASCADE | سلوك Supabase JS client عبر REST قد يكون غير متسق مع RLS في بعض الإعدادات — الحذف الصريح كحماية إضافية |
| فحص COUNT لكل منتج/مستخدم على حدة (بدون استعلام SQL خام) | تجنب الحاجة لـ DDL/migrations — التطبيق الحالي لا يسمح بإضافة SQL functions بسهولة |
| Dry Run (`CRON_DRY_RUN=true`) كآلية أمان افتراضية | يسمح لصاحب المحل بتأكيد ما سيُحذف قبل التنفيذ الفعلي — تشغيله بدون خوف في أي وقت |
| مصادقة Bearer Token بدلاً من Vercel-native cron | المشروع لم يُنشر بعد على Vercel — آلية عامة تعمل مع أي موفر cron خارجي |
| دالة `runCleanup()` تجمع كل الخطوات في استدعاء واحد | تبسيط استدعاء الـ route وتوحيد نقطة الدخول |

### نتائج الاختبار

- ✅ Dry Run (`CRON_DRY_RUN=true`) يعمل ويعيد JSON كامل بعدد العناصر بدون حذف فعلي
- ✅ `Authorization: Bearer` يُرفض الطلب بدون توكن (401) أو بتوكن خاطئ (401)
- ✅ الطلب الصحيح مع التوكن السليم يمر ويستدعي runCleanup
- ✅ عند عدم وجود بيانات قديمة، يرجع صفر في كل الفئات بدون أخطاء
- ✅ `npm run build` ناجح بدون أخطاء TypeScript أو compilation

### الخطوة التالية

بمجرد التأكد من استقرار النظام وعدم الحاجة لاسترجاع أي بيانات قديمة:
1. **تعديل `.env.local`:** تغيير `CRON_DRY_RUN=false` (أو حذف المتغير كليًا)
2. **نشر المشروع** على Vercel أو استضافة أخرى
3. **ربط cron خارجي:** إضافة مهمة يومية في cron-job.org أو أي موفر مع الأمر:
    `curl -X POST https://your-domain.com/api/cron/cleanup -H "Authorization: Bearer <CRON_SECRET>"`

---

## يوليو 2026 — Cron: حذف المنتجات المعطلة اليتيمة (غير النشطة فقط) ✅

**التغيير:** تعديل `deleteOrphanProducts` في `lib/cleanup/archive-cleanup.ts` لاستهداف المنتجات غير النشطة (`is_active = false`) فقط، بدلاً من جميع المنتجات.

**المنطق:**
- بعد حذف الورديات القديمة (ومعها `sessions`/`sale_items`) مباشرة، يبحث عن المنتجات التي:
  - `is_active = false`
  - ليس لها أي صف في `sale_items` (عدد = 0)
- يُسجّل في console أسماء المنتجات اليتيمة أثناء Dry Run
- في الوضع الفعلي: يحذفها hard-delete من جدول `products`
- `is_active = true` لا تُمس أبدًا، بغض النظر عن تاريخ المبيعات

**الحماية:** `CRON_DRY_RUN=true` لا يزال مفعّلًا — لن يحدث أي حذف فعلي حتى يتم تغييره إلى `false` يدويًا.

**الملف المتأثر:** `lib/cleanup/archive-cleanup.ts`

**التحقق:** ✅ `npm run build` ناجح بدون أخطاء

---

## يوليو 2026 — تفعيل Cron Vercel (جدولة يومية للتنظيف) ✅

**ما تم:** إضافة ملف `vercel.json` في جذر المشروع يحتوي على تعريف Cron Job لمهمة التنظيف اليومية.

**المحتوى:**
```json
{
  "crons": [
    {
      "path": "/api/cron/cleanup",
      "schedule": "0 3 * * *"
    }
  ]
}
```

- **الجدول:** 3:00 AM UTC يوميًا (`0 3 * * *`)
- **التفعيل:** بعد الـ push والتdeploy التالي على Vercel — لا يمكن اختباره محليًا

**🔧 إصلاح التوثيق:** تم تعديل `app/api/cron/cleanup/route.ts` لقبول مسارين توثيق منفصلين:
- الأولوية الأولى: هيدر `x-vercel-cron: 1` (Vercel native cron) — يمر فورًا بدون أي اعتماد على `CRON_SECRET` أو `Authorization` هيدر
- Fallback: `Authorization: Bearer <CRON_SECRET>` (للاختبار اليدوي وموفري cron الخارجيين)
- `authHeader` و `expectedSecret` معلنين مرة واحدة في أعلى الدالة، يستخدمهم مسار الـ Bearer فقط

**الملف:** `vercel.json` (جديد)

**التحقق:** ✅ `npm run build` ناجح (لا يتأثر بـ vercel.json)

---

## يوليو 2026 — إضافة الشعار بجانب "PS-System" في رأس السايد بار ✅

**ما تم:** إضافة الشعار (`/logo-ps.svg`) في رأس السايد بار بجانب عنوان "PS-System".

**التفاصيل:**
- الشعار موجود مسبقًا في `public/logo-ps.svg` (تم نسخه سابقًا من `assets/`)
- أضيف كـ `<img src="/logo-ps.svg">` في `components/layout/sidebar.tsx` قبل نص "PS-System" مباشرة
- المقاس: `h-8 w-8` (32×32px)
- الشعار لا يحتوي على gradients (PNG مضمن في SVG كـ base64)
- تم تحديث `TECH_INSTRUCTIONS.md` القسم 6.7 البند 1 لذكر الشعار

**الملف المتأثر:** `components/layout/sidebar.tsx`

**التحقق:** ✅ `npm run build` ناجح

---

## يوليو 2026 — تأكيد سياسة الحذف الذكي للمنتجات (موجودة مسبقًا) + توثيق TECH_INSTRUCTIONS.md

**التحقق:** المنطق المطلوب (فحص `sale_items` أولاً ثم hard delete عند count=0 أو soft delete (`is_active=false`) عند count>0) موجود بالفعل في `app/api/products/[id]/route.ts:144-189`. لا توجد أي toggle منفصلة لـ `is_active` في الواجهة.

**الإضافات التوثيقية فقط:**
- إضافة قسم 6.9 جديد في `TECH_INSTRUCTIONS.md` — "سياسة حذف المنتجات (إدارة المشروبات)" يشرح السلوك والقواعد الصارمة
- إعادة ترقيم قسم المهمة المجدولة من 6.9 إلى 6.10

**لا تغيير في الكود** — السياسة كانت مطبقة بالفعل.

---

## يوليو 2026 — إصلاح: تفعيل/تعطيل الأجهزة لم يكن يُطبّق على الصفحة الرئيسية + إصلاحات UI

**Part 1 — التحقيق:** زر التفعيل/التعطيل في الإعدادات كان يعمل (API ينفّذ، DB يُحدّث، الـ UI المحلي يتغير)، لكن:
- **`GET /api/stations/list`** لم يكن يفحص `shops.ps_enabled`/`billiard_enabled`/`pingpong_enabled`
- **`POST /api/sessions/start`** لم يكن يفحصها أيضًا
- النتيجة: الأجهزة المعطلة تظهر في الصفحة الرئيسية ويمكن بدء جلسات عليها

**Part 2 — الإصلاحات:**
1. **`app/api/stations/list/route.ts`** — جلب إعدادات المحل (`shops.ps_enabled` إلخ) وتصفية المحطات التي نوعها معطّل. المحطات المعطلة لا تعود من API أصلًا.
2. **`app/api/sessions/start/route.ts`** — بعد التحقق من وجود الجهاز، يفحص هل نوعه مفعّل في إعدادات المحل. إذا لا: يرجع `400` برسالة عربية: "البلايستيشن/البلياردو/البينغ بونغ معطل حاليًا من الإعدادات"

**Part 3 — إصلاحات UI:**
1. **Toast position** (`components/ui/toast.tsx`): تغيير `top-4` إلى `top-20 sm:top-4` لمنع تداخل الإشعارات مع الهيدر اللاصق (64px) على الموبايل. كما رفع `z-50` إلى `z-[100]` لضمان الظهور فوق كل العناصر.
2. **Settings card overflow** (`components/settings/ps-settings-form.tsx`، `billiard-settings-form.tsx`، `pingpong-settings-form.tsx`): تغيير `flex gap-2` إلى `flex flex-col gap-2 sm:flex-row` في قسم عدد الأجهزة/الطاولات لمنع تجاوز المحتوى لحدود الكارد على الشاشات الضيقة. الإدخال والزر أصبحا `w-full` على الموبايل و `flex-1`/`w-auto` على الدسكتوب.

**الملفات المتأثرة:**
- `app/api/stations/list/route.ts`
- `app/api/sessions/start/route.ts`
- `components/ui/toast.tsx`
- `components/settings/ps-settings-form.tsx`
- `components/settings/billiard-settings-form.tsx`
- `components/settings/pingpong-settings-form.tsx`

**التحقق:**
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ أجهزة البلايستيشن تختفي من الصفحة الرئيسية عند تعطيلها في الإعدادات
- ✅ محاولة بدء جلسة على جهاز معطل → رسالة "(النوع) معطل حاليًا من الإعدادات"
- ✅ التوست يظهر تحت الهيدر على الموبايل (وليس فوقه)
- ✅ محتوى كارد عدد الأجهزة لا يتجاوز حدوده على الشاشات الضيقة

---

## يوليو 2026 — تنظيف بيانات التطوير والبدء بالإنتاج الفعلي ✅

**ما تم:** حذف كل بيانات التطوير والاختبار من قاعدة البيانات بناءً على خطة معتمدة، مع الإبقاء على بيانات الإنتاج الحقيقية.

### المحذوفات

| الكيان | العدد | التفاصيل |
|---|---|---|
| الورديات المقفولة | 6 | من 12 يوليو إلى 18 يوليو 2026 — جميعها data test |
| الجلسات | 19 | مرتبطة بالورديات المحذوفة |
| مبيعات المشروبات | 13 | مرتبطة بالورديات المحذوفة |
| المصاريف | 0 | لم تكن موجودة |
| المنتجات غير النشطة | 4 | شاي (2×)، نسكافية، قهوة — is_active=false |
| المحطات المكررة المعطلة | 4 | جهاز 5 (2×)، جهاز 6 (2×) — is_active=false |
| المستخدمين الموظفين | 2 | محمد احمد (DJBSZM) + zzm2469 (UMQLVN) — بعد التأكد من عدم وجود ورديات مرتبطة |

### المتبقي

| الكيان | العدد | التفاصيل |
|---|---|---|
| المحل | 1 | Super Remontada |
| المستخدم المالك | 1 | OWNER1 (ziad mohamed) |
| الوردية المفتوحة | 1 | #1 — مفتوحة حاليًا (لم تُمس) |
| المنتجات النشطة | 6 | بيبسي، شاي، فيوري بلاستيك، فيوري كنز، قهوة، نسكافية |
| المحطات الفعالة | 12 | PS1–PS4 + طاولة 1–4 (بلياردو) + طاولة 1–4 (بينغ بونغ) |
| قواعد التسعير | 12 | بدون تغيير |

### حالة النظام

النظام جاهز لبدء الإنتاج الفعلي — جميع بيانات الاختبار قد أزيلت، والتجهيزات (الأسعار، الأجهزة، المحل، حساب المالك) محفوظة كما هي. يمكن البدء بإدخال بيانات حقيقية فورًا.

---

## يوليو 2026 — إزالة بقايا قفل الوردية الملغي (Shift Lock Cleanup) ✅

**الخلفية:** قرار "قفل التعديل أثناء الوردية المفتوحة" أُلغي رسميًا في يوليو 2026 (موثق في TECH_INSTRUCTIONS.md §2.5). جميع نقاط الـ API لا تطبق أي قفل متعلق بحالة الوردية. لكن ملف الأداة المساعدة `hasOpenShift()` ظل موجودًا في قاعدة الكود بدون استخدام.

**ما تم إزالته:**

| الملف | السبب |
|---|---|
| `lib/shifts/check-open-shift.ts` | دالة `hasOpenShift()` — غير مستخدمة في أي مكان (لا UI ولا API) — أثر من القرار الملغي |

**ما لم يتم العثور عليه:** لا يوجد أي نص تحذيري في واجهة المستخدم يشير إلى "الحقول محمية أثناء الوردية المفتوحة" أو ما شابه — جميع مكونات الإعدادات (`shifts-settings-form`, `ps-settings-form`, `billiard-settings-form`, `pingpong-settings-form`, `products-settings-form`, `settings-form`) خالية تمامًا من أي إشارة إلى هذا القيد الملغي.

**نتائج التحقق:**
- ✅ `lib/shifts/check-open-shift.ts` — محذوف (غير مستخدم)
- ✅ لا توجد إشارات UI قديمة للقيد الملغي
- ✅ جميع الحقول قابلة للتعديل كما هي (لم يتغير任何 منطق)
- ✅ `npm run build` ناجح بدون أخطاء

---

## يوليو 2026 — حذف الوردية المفتوحة السابقة — اكتمال التنظيف ✅

**ما تم:** الوردية الوحيدة المتبقية (كانت مفتوحة سابقًا، أغلقها صاحب المحل) تم حذفها مع بياناتها، ليكتمل تنظيف قاعدة بيانات التطوير بالكامل.

**الوردية المحذوفة:**
| الحقل | القيمة |
|---|---|
| shift_number | 1 |
| responsible_name | زياد |
| الحالة | closed |
| opened_at | 2026-07-19 19:25 |
| closed_at | 2026-07-19 20:11 |
| الجلسات المرتبطة | 1 |
| مبيعات المشروبات | 1 |

**ملاحظة المنتجات:** بعد الحذف، جميع المنتجات النشطة (6) أصبحت بدون مبيعات (`sale_items = 0`). هذا متوقع — البيانات الوحيدة الموجودة كانت ضمن الوردية المحذوفة. المنتجات نفسها سليمة وجاهزة لاستقبال مبيعات جديدة عند بدء التشغيل الفعلي.

**الوضع النهائي لقاعدة البيانات:**
| الكيان | العدد |
|---|---|
| الورديات | 0 |
| الجلسات | 0 |
| مبيعات المشروبات | 0 |
| المصاريف | 0 |
| المستخدمون | 1 (OWNER1 — ziad mohamed) |
| المنتجات النشطة | 6 |
| المحطات الفعالة | 12 |
| قواعد التسعير | 12 |

**قاعدة البيانات الآن في حالة نظيفة تمامًا وجاهزة لبدء الإنتاج الفعلي.**

---

## يوليو 2026 — Code Cleanup via clean-code-guard + test-guard ✅

**الحراس المطبقون:**
- `clean-code-guard` — مراجعة شاملة لقاعدة الكود وفق 24 مبدأ (أسماء دوال، تعليقات، SOLID، DRY/KISS/YAGNI، AI-specific guardrails)
- `test-guard` — مراجعة ملفات الاختبار (لم يتم العثور على أي ملفات اختبار `.test.*` أو `.spec.*`)

### ما تم تغييره (آمن، لا يؤثر على السلوك):

| الملف | التغيير |
|---|---|
| `lib/pricing/calculation.ts:31` | إزالة `station_type` و `mode` من destructuring (غير مستخدمين في جسم الدالة) |
| `components/settings/products-settings-form.tsx:3` | إزالة استيراد `useEffect` غير المستخدم |
| `components/settings/products-settings-form.tsx:76-105` | إزالة دالة `handleUpdateProduct` الميتة (معرفة ولكن غير مستدعاة) |
| `components/ui/toast.tsx:4` | إزالة استيراد `useEffect` غير المستخدم، نقل `import React` إلى أعلى الملف |
| `components/sessions/stations-grid.tsx:7-12` | إزالة prop `shopId` غير المستخدم من الواجهة والمكون والمستدعي |
| `app/page.tsx:96` | إزالة `shopId` من استدعاء `StationsGrid` (مطابق للتغيير أعلاه) |
| `components/settings/team-form.tsx:8,22,39,194,197,201` | إزالة prop `shopId` غير المستخدم، دمج `activeMembers` ← `teamMembers` |
| `app/settings/team/page.tsx:89` | إزالة `shopId` من استدعاء `TeamManagementForm` |

### ما تم الإبلاغ عنه ولكن تُرك دون تغيير (يحتاج موافقة):

| الملف:السطر | السبب |
|---|---|
| `components/layout/authenticated-shell.tsx:56,134` | ToastProvider مكرر (متداخل مرتين) — إزالة واحدة آمنة لكنها تمس موفر سياق |
| `app/page.tsx:79-84` | `title`/`subtitle` محسوبان لكن غير مستخدمين في مسار المصادق — يحتاج إعادة هيكلة |
| `app/api/sessions/remove-product/route.ts` | نقص `assertPermission()` — ثغرة أمنية تحتاج موافقة قبل الإصلاح |
| `lib/auth/session.ts:45-48` | `getSessionSecret()` يقع fallback إلى `SUPABASE_SERVICE_ROLE_KEY` — خلط أسرار |
| `lib/auth/session.ts:54-62` | `verifySessionCookieValue` يبتلع كل الأخطاء بصمت |
| تكرار `getCurrentUser`/`getShopName`/`getOwnerName` في 10+ صفحة Server | تكرار معماري — يستحق استخراج helper مشترك |
| `app/api/stations/list/route.ts:42-43` | `as any` casts على `stationType` |
| `components/sessions/session-popup.tsx` | 570 سطر + مشاكل hoisting (`fetchProducts`, `fetchSaleItems`) — إعادة الترتيب تغير السلوك |
| `lib/cleanup/archive-cleanup.ts` | N+1 queries في `deleteOrphanProducts()` — تحسين أداء لكنه ليس تجميليًا |

### نتيجة التحقق:
- ✅ `npm run build` (next build) — ناجح بدون أخطاء TypeScript أو compilation
- ✅ `npm run lint` — يمر (جميع المشاكل المتبقية كانت موجودة قبل التغيير، أو تتطلب تغيير سلوك لإصلاحها)

---

## يوليو 2026 — Bug: رسالة "Failed to get open shift" عند بدء جلسة بدون وردية ✅

### الخلفية
عندما يضغط المستخدم على جهاز (station) لبدء جلسة جديدة **ولا توجد وردية مفتوحة حاليًا**، كان النظام يعرض رسالة خطأ تقنية خام بالإنكليزية: `"Failed to get open shift"`. يبدو هذا كفشل في API، بينما الواقع ببساطة أنه "لا توجد وردية مفتوحة".

### السبب الجذري
في `app/api/sessions/start/route.ts:116-121`، كان الشرط `if (shiftError || !openShift)` يدمج حالتين مختلفتين:
1. **`shiftError`** — فشل حقيقي في قاعدة البيانات (خطأ تقني)
2. **`!openShift`** — لا توجد وردية مفتوحة (حالة عمل متوقعة)

كلاهما كان يُعيد `{ error: "Failed to get open shift" }` مع status 500، والواجهة تعرض الرسالة كما هي.

### الإصلاح
فصل الشرط إلى حالتين:
- **`shiftError`** ← إعادة `500` مع رسالة تقنية (لم تتغير، تحتفظ بالرسالة القديمة + `console.error`)
- **`!openShift`** ← إعادة `400` مع رسالة عربية واضحة: `"لازم تفتح وردية الأول قبل ما تقدر تبدأ جلسة"`

الواجهة (`session-popup.tsx`) تظهر رسالة الخطأ من API مباشرة في Toast، فتصبح الرسالة العربية هي ما يراه المستخدم.

### الملف المتأثر
- `app/api/sessions/start/route.ts` — فصل الشرط وإضافة رسالة عربية

### التحقق
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ (أ) بدء جلسة بدون وردية مفتوحة → يعرض Toast: "لازم تفتح وردية الأول قبل ما تقدر تبدأ جلسة" (بدلاً من "Failed to get open shift")
- ✅ (ب) بدء جلسة مع وردية مفتوحة → يعمل بشكل طبيعي (نفس الكود، لم يتغير المنطق بعد التحقق من الوردية)
- ✅ لا تغيير في سلوك فتح/قفل الورديات
- ✅ لا تغيير في سلوك Session Popup (يبقى مفتوحًا، المستخدم يقرأ الرسالة ويقفل البوب أب بنفسه)

---

## يوليو 2026 — تطبيق Migration 002_expenses_add_fields.sql على قاعدة البيانات المباشرة ✅

### محتوى الملف (تمت المراجعة والتأكيد)
ملف `002_expenses_add_fields.sql` (أُعيد تسميته إلى `012_expenses_add_fields.sql` لحل تعارض التسمية) يطبق التغييرات التالية على جدول `expenses`:
1. `ALTER COLUMN shift_id DROP NOT NULL` — يجعل `shift_id` قابلًا للقيم الفارغة
2. `ADD COLUMN category TEXT` — يضيف عمود الفئة
3. `ADD COLUMN expense_date DATE NOT NULL DEFAULT CURRENT_DATE` — يضيف عمود التاريخ مع قيمة افتراضية لليوم الحالي
4. إنشاء `INDEX` على `expense_date`

### حالة ما قبل التطبيق
- `category` column → ❌ غير موجود
- `expense_date` column → ❌ غير موجود
- `shift_id` nullable → ❌ لا يزال NOT NULL
- عدد الصفوف: 0

### التطبيق
- تم تطبيق SQL عبر Supabase Management API (201 Created)
- تم إصلاح سجل الهجرة (migration repair) لتسجيل `012` كمنفّذ
- تم حل تعارض التسمية: `002_expenses_add_fields.sql` ← `012_expenses_add_fields.sql` (كان هناك ملفان بنفس البادئة `002`)

### التحقق بعد التطبيق — `information_schema.columns` لجدول `expenses`:

| column_name | is_nullable | data_type |
|---|---|---|
| `id` | NO | uuid |
| `shop_id` | NO | uuid |
| `shift_id` | **YES** | uuid |
| `description` | NO | text |
| `amount` | NO | numeric |
| `created_at` | NO | timestamp with time zone |
| `category` | **YES** | text |
| `expense_date` | **NO** | date |

- ✅ `category` column موجود
- ✅ `expense_date` column موجود
- ✅ `shift_id` قابل للقيم الفارغة
- ✅ عدد الصفوف قبل وبعد: 0 ← 0 (لا فقدان بيانات)
- ✅ جميع الهجرات متزامنة (Local = Remote): 001 → 012 كلها متطابقة

---

## يوليو 2026 — إصلاح أمني: إزالة `shift_id` من طلب العميل في إنشاء المصاريف ✅

**الخلفية:** عند إنشاء مصروف جديد، كان العميل يرسل `shift_id` في Body الطلب (بعد جلبه من `/api/shifts/current`)، وكان السيرفر يستخدم هذا الـ `shift_id` كما هو دون تحقق — مما يسمح للمستخدم بتزوير الـ shift_id.

**الإصلاح:**

1. **الواجهة (`components/expenses/expenses-list.tsx`):**
   - إزالة الـ Pre-flight call إلى `/api/shifts/current` (كان يضيف رحلة ذهاب وإياب زائدة)
   - إزالة `body.shift_id` تمامًا من طلب POST — السيرفر الآن يتولى كل شيء في رحلة واحدة

2. **السيرفر (`app/api/expenses/route.ts`):**
   - لم يعد يستقبل `shift_id` من جسم الطلب (أزيل من الـ destructuring)
   - دائماً يحل `shift_id` من الوردية المفتوحة Server-side
   - إذا لم توجد وردية مفتوحة، يرفض الطلب بـ `400` مع رسالة الخطأ العربية

**الميزة:** رحلة شبكة واحدة بدلاً من اثنتين + السيرفر هو المصدر الوحيد للحقيقة للـ shift_id.

**نتائج التحقق:**
- ✅ `npm run build` — ناجح بدون أخطاء TypeScript أو compilation
- ✅ إنشاء مصروف مع وردية مفتوحة → يعمل بشكل طبيعي
- ✅ إنشاء مصروف بدون وردية مفتوحة → يعرض Toast: "لا يمكن إضافة مصروف خارج الوردية. يرجى فتح وردية أولاً."

**الإصلاح نفسه طُبق على PUT:**
- `app/api/expenses/[id]/route.ts` — إزالة `shift_id` من destructuring في Body الطلب ومن كود التحديث. الـ `shift_id` لا يتغير أبدًا عند تعديل المصروف؛ يبقى كما هو من وقت الإنشاء.
- ✅ تعديل مصروف لا يغير `shift_id` المرتبط به أبدًا

**إصلاح ترميز:**
- `PROGRESS.md` — تصحيح `في同步` إلى `متزامنة` (حروف صينية مختلطة بالنص العربي).

---

## يوليو 2026 — توحيد رسائل العربية إلى الفصحى الرسمية ✅

**الخلفية:** كانت بعض رسائل الخطأ والتسميات في الواجهة مكتوبة بالعامية المصرية، مما يسبب عدم تناسق مع بقية التطبيق.

**الإصلاحات:**

| الملف | قبل | بعد |
|---|---|---|
| `app/api/sessions/start/route.ts:161` | لازم تفتح وردية الأول قبل ما تقدر تبدأ جلسة | لا يمكن بدء جلسة بدون وردية مفتوحة. يرجى فتح وردية أولاً. |
| `app/api/shifts/close/route.ts:87` | لا يزال هناك أجهزة شغالة. يجب إغلاق جميع الجلسات قبل إنهاء الوردية. | لا تزال هناك أجهزة نشطة. يجب إغلاق جميع الجلسات قبل إنهاء الوردية. |
| `components/sessions/station-card.tsx:180` | شغال (status label) | نشط (status label) |

**غير متأكد (يحتاج قرارك):**
- `app/(auth)/login/page.tsx:124` — `"هل نسيت ال-ID؟"` تستخدم `ال-` مع كلمة إنجليزية ID. قد يكون مقصوداً كـ UX غير رسمي.

**نتائج التحقق:**
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ clean-code-guard: clean (لا تغييرات في المنطق)
- ✅ test-guard: N/A (لا تغييرات في ملفات الاختبار)

---

## يوليو 2026 — تحديث العلامة التجارية لصفحة تسجيل الدخول ✅

**الإصلاحات:**

| الملف | قبل | بعد |
|---|---|---|
| `app/(auth)/login/page.tsx:74` | نظام إدارة المحل | نظام إدارة محلات الألعاب والترفيه |
| `app/(auth)/login/page.tsx:56-71` | أيقونة SVG placeholder | شعار `/logo-ps.svg` (56px) |
| `app/layout.tsx:13` | نظام إدارة محل بلايستيشن (title) | PS-System - نظام إدارة محلات الألعاب |

**نتائج التحقق:**
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ clean-code-guard: clean (تغييرات تجميلية فقط)
- ✅ test-guard: N/A (لا تغييرات في ملفات الاختبار)

---

## يوليو 2026 — إصلاح تجاوز المحتوى في صفحة إدارة المشروبات على الجوال ✅

**السبب الجذري:** بطاقة "إضافة مشروب جديد" تستخدم `flex gap-2` في جميع أحجام الشاشات، مع وجود 3 عناصر (حقل اسم + حقل سعر `w-32` 128px + زر `min-w-[100px]`). على شاشات الجوال (360px)، يتجاوز الحد الأدنى لعرض الصف (≈324px) المساحة المتاحة (≈280px)، مما يسبب تجاوزاً أفقياً للصفحة بأكملها.

**الإصلاح:**
- تغيير `flex gap-2` إلى `flex flex-col gap-2 sm:flex-row` — رصّ العناصر عمودياً على الجوال
- تغيير `flex-1` إلى `w-full sm:flex-1` لحقل الاسم
- تغيير `w-32` إلى `w-full sm:w-32` لحقل السعر
- إضافة `w-full sm:w-auto` و `sm:min-w-[100px]` للزر

النمط المستخدم مطابق تماماً للنمط الموجود في `ps-settings-form.tsx` و `billiard-settings-form.tsx` و `pingpong-settings-form.tsx` (الملفات المجاورة).

**ملاحظة:** no viewport meta tag كان مفقوداً، ولكن لأن Next.js يضيف viewport meta tag افتراضياً (`width=device-width, initial-scale=1`)، فإن السبب الحقيقي كان تجاوز عناصر الصف للحدود. لا حاجة لتعديل viewport عالمي.

**نتائج التحقق:**
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ clean-code-guard: clean
- ✅ test-guard: N/A

---

## يوليو 2026 — إصلاح حقل الرقم: Placeholder بدلاً من القيمة الافتراضية 0 ✅

**السبب:** حقول الإدخال الرقمية (`<input type="number">`) كانت تهيئ حالة React بقيمة رقمية `0`، مما يجعل الحقل يظهر ممتلئاً بالرقم "0" بدلاً من أن يكون فارغاً مع نص توجيهي. هذا يسبب ارتباكاً على الجوال حيث لا تظهر القيمة الافتراضية كنص توجيهي.

**الإصلاح:** تغيير نوع الحالة من `number` إلى `string`، مع تهيئتها بقيمة فارغة `""`، وتحويل القيمة وقت الحفظ فقط. هذا يسمح بظهور `placeholder` بشكل صحيح.

**الحقول التي تم تغييرها (17 حقل عبر 5 ملفات):**

| الملف | الحقل | الحالة السابقة | الحالة الجديدة |
|---|---|---|---|
| `ps-settings-form.tsx` | عدد أجهزة البلايستيشن | `number \| 0` / placeholder "0" | `string` / placeholder "العدد" |
| `ps-settings-form.tsx` | 4 أسعار (فردي/مالتي × ساعة/جيم) | `number \| 0` / placeholder "0.00" | `string` / placeholder "السعر" |
| `billiard-settings-form.tsx` | عدد طاولات البلياردو | `number \| 0` / placeholder "0" | `string` / placeholder "العدد" |
| `billiard-settings-form.tsx` | 4 أسعار (فردي/مالتي × ساعة/جيم) | `number \| 0` / placeholder "0.00" | `string` / placeholder "السعر" |
| `pingpong-settings-form.tsx` | عدد طاولات البينغ بونغ | `number \| 0` / placeholder "0" | `string` / placeholder "العدد" |
| `pingpong-settings-form.tsx` | 4 أسعار (فردي/مالتي × ساعة/جيم) | `number \| 0` / placeholder "0.00" | `string` / placeholder "السعر" |
| `shifts-settings-form.tsx` | عدد الورديات اليومية | `number` (initial 1-4) | `string` / placeholder "العدد" |
| `expenses-list.tsx` | المبلغ في نموذج المصروفات | placeholder "0" (الحالة كانت string أصلاً) | placeholder "المبلغ" |

**ملاحظات:**
- `products-settings-form.tsx`: كان الحقلان أصلاً من نوع `string` مع `placeholder="السعر"` — لا حاجة للتغيير.
- `session-popup.tsx`: حقول `gamesCount` و `gamesCountInput` و `selectedQuantity` لها قيم افتراضية منطقية (1 للكمية، عدد الجيمات الفعلي للجلسة) — لا حاجة للتغيير.
- دالتا `handleSavePricing` و `handleSaveStationCount`/`handleSaveTableCount`/`handleSave` تم تحديثها لتحويل النص إلى رقم قبل الحفظ.
- أزرار الحفظ: تم تحديث مقارنات `===` للمقارنة الرقمية بعد تحويل النص.

**لم يتم تغيير (يحتاج قرارك):** صفوف التسعير (`<div className="flex gap-2">`) في ملفات PS/Billiard/Pingpong تستخدم `flex-1 input + min-w-[100px] button` — تعمل على معظم أحجام الجوال بعنصرين فقط، ولكن يمكن تحسينها بنمط `flex flex-col gap-2 sm:flex-row` للاتساق.

**نتائج التحقق:**
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ clean-code-guard: clean (تغييرات حالة + واجهة فقط، لا منطق)
- ✅ test-guard: N/A

---

## يوليو 2026 — توثيق: نظام أنواع اللعب في البلياردو (Play Type) — توثيق فقط ✅

**التغيير:** تحديث وثائقي فقط — لا كود تطبيق تم لمسه. تم تعديل ملفين توثيق فقط:

### BRD_PS_System.md (فيتشر 5)
- إضافة blockquote جديد بعد سطر `**الوصف:**` يشرح نظام أنواع اللعب المستقل عن الفوترة: عادي (فردي/مالتي) وكومب (فردي/متولتة/مربعة)
- إضافة 2 user stories جديدة بعد بند تسجيل الجيمات: (1) اختيار نوع اللعب عند فتح الجلسة، (2) استقلالية طريقة الفوترة عن نوع اللعب

### TECH_INSTRUCTIONS.md
- إضافة قسم جديد **6.7) نظام أنواع اللعب في البلياردو (Play Type)** بعد قسم 6.6 (Session Popup) — يشرح: 5 أنواع لعب، استقلالية billing_mode، 10 صفوف تسعير موحّدة للمحل، قواعد صارمة (تثبيت play_type عند الفتح، اختيارات متتالية في البوب أب، تحديث صفحة /settings/billiard لـ 10 أسعار، لا تأثير على PS/Pingpong)
- إعادة ترقيم الأقسام: السايد بار (6.7←6.8)، إضافة الموظف (6.8←6.9)، سياسة الحذف (6.9←6.10)، المهمة المجدولة (6.10←6.11)
- تحديث المراجع المتبادلة (قسم 6.7 في التوب بار والسايد بار ← 6.8، قسم 6.10 ← 6.11)
- تحديث وصف `pricing_rules` في القسم 3 ليعكس هيكل التسعير الجديد (10 صفوف للبلياردو)

### لم يتغير:
- لا كود تطبيق (API، UI، صفحات، مكونات) تم تعديله أو إنشاؤه
- لا ملفات قاعدة بيانات أو migrations تمت
- جميع الميزات الحالية تعمل كما هي

### الخطوة التالية (خارج نطاق هذا التحديث — يُحدَّد في prompt لاحق):
- تنفيذ التغيير في schema pricing_rules (من 4 إلى 10 صفوف للبلياردو)
- تحديث صفحة `/settings/billiard` لعرض 10 أسعار
- إضافة `play_type` / `play_subtype` إلى session popup للبلياردو

### تصحيح — يوليو 2026
- إصلاح وصف pricing_rules في §3: تغيير "فردي/مالتي بالوقت" إلى "الوضعين معًا (فردي/مالتي × وقت/عدد جيمات)" للبلايستيشن/البينغ بونغ
- إصلاح القاعدة 4 في §6.7: توضيح أن PS/Pingpong يبقيان فردي/مالتي × وقت/عدد جيمات (وليس "بالوقت فقط")

---

## يوليو 2026 — توثيق: نظام سطور الجيمات المتراكمة للبلياردو (لعبة/جيمات) — توثيق فقط ✅

**التغيير:** تحديث وثائقي فقط — لا كود تطبيق تم لمسه. تم تعديل ملفين توثيق:

### BRD_PS_System.md (فيتشر 5)
- إضافة blockquote جديد بعد blockquote أنواع اللعب يشرح نموذج السطور المتراكمة للبلياردو بوضع "عدد جيمات": نوع اللعب يُحدَّد لكل دفعة إضافة جيمات على حدة (وليس مرة واحدة للجلسة)، مع Snapshot سعري لكل سطر لا يتأثر بالتغييرات اللاحقة.
- إضافة 3 user stories جديدة: (1) فتح جلسة بلياردو بعدد جيمات بدون إلزام بنوع لعب، (2) إضافة دفعات جيمات بأنواع لعب مختلفة على نفس الجلسة، (3) عدم تأثر الجلسات المفتوحة بتغيير الأسعار لاحقًا.

### TECH_INSTRUCTIONS.md
- استبدال القسم 6.7 بالكامل بنسخة محدَّثة تشرح: وضع "بالوقت" (نوع لعب ثابت عند الفتح كالمعتاد) vs وضع "بعدد الجيمات" (نموذج سطور متراكمة مع Snapshot سعري)، إضافة تعريف جدول `billiard_game_entries`، تحديث القواعد الصارمة لتعكس النموذج الجديد.
- إضافة جدول `billiard_game_entries` إلى القسم 3 (هيكل الجداول).

### لم يتغير:
- لا كود تطبيق (API، UI، صفحات، مكونات) تم تعديله أو إنشاؤه.
- لا ملفات قاعدة بيانات أو migrations تمت.
- جميع الميزات الحالية تعمل كما هي.

### الخطوة التالية (خارج نطاق هذا التحديث — يُنفَّذ في جلسة لاحقة بخطة معتمدة):
- بناء `billiard_game_entries` migration + نموذج API + UI للجلسات.

## يوليو 2026 — تنفيذ البلياردو بالسطور المتراكمة (5 مراحل) + تصحيح ✅

**المراحل المنجزة:**
1. **الـ Migration (013_billiard_game_entries.sql):** جدول `billiard_game_entries` + أعمدة `play_type`/`play_subtype` في `pricing_rules` و`sessions` + 10 صفوف تسعير بلياردو (عادي/كومبو × فردي/مالتي/ثنائي/رباعي × ساعة/جيم) + تغيير UNIQUE constraint.
2. **الأنواع (Types):** `PlayType`، `PlaySubtype`، `BilliardGameEntry` في `types/database.ts` و`index.ts`.
3. **التسعير (Pricing):** دوال `calculateGameEntrySubtotal` و `calculateBilliardGameEntriesCost` في `calculation.ts` — المصدر الوحيد للحقيقة.
4. **الـ API Routes:** 5 مسارات (`/add-game-entry`، `/game-entries`، تحديث `start`/`preview-close`/`confirm-close`).
5. **الواجهة (UI):** إعدادات 10 حقول تسعير، عرض الجيمات المسجلة في session-popup مع إضافة دفعات جديدة، إظهار `game_entries_cost` في receipt-popup.

**تصحيح لاحق:**
- تم اكتشاف أن `session-popup.tsx` كانت تحسب `games_count * price_per_game` يدويًا في سطرين (عرض السطر والمجموع) بدل استخدام دالة التسعير المركزية. تم إصلاحه باستيراد `calculateGameEntrySubtotal` و `calculateBilliardGameEntriesCost` من `calculation.ts`. لم يعد أي ملف يحسب هذه القيمة يدويًا خارج `calculation.ts`.
- تم حذف `if` بلوك فارغ (ميت) في `start/route.ts`.
- تم إعادة تسمية الدالة `calculateSingleGameEntryCost` → `calculateGameEntrySubtotal` للوضوح.
- تم تشغيل `clean-code-guard`: 3 إصلاحات، 0 ملاحظة.
- تم تشغيل `test-guard`: لا يوجد إطار اختبار في المشروع لتنفيذ توصيات الاختبارات.

**ملاحظة:** أسعار الكومبو (combo) هي قيم وهمية (placeholders) — يجب على صاحب المحل مراجعتها وتعديلها في `/settings/billiard`.

---

## يوليو 2026 — إصلاحات المجموعة A + B (شاملة) ✅

### A1 — تحويل `type="number"` إلى `type="text"` مع `inputMode="numeric"`
- **الحالة:** كان مُطبَّقًا بالفعل ✅ — لم يتم العثور على أي `<input type="number">` في أي ملف .tsx/.ts في المشروع (فقط إشارات توثيقية في PROGRESS.md).

### A2 — ركود واجهة عرض عدد الجيمات في البلياردو بعد حفظ الدفعة
- **المشكلة:** العنوان الرئيسي في session-popup كان يعرض `{gamesCount}` المستمد من `activeSession?.games_count`، والذي لا يتغير عند إضافة دفعات جيمات جديدة (الـ API لا يحدّث `sessions.games_count`).
- **الإصلاح:** تغيير العرض لاستخدام `gameEntries.reduce((sum, e) => sum + e.games_count, 0)` لجلسات البلياردو (billiard + games)، ليعكس إجمالي الجيمات المتراكمة من جميع الدفعات المسجلة. Types الجلسات الأخرى (غير بلياردو) لا تزال تستخدم `gamesCount` كما كانت.
- **الملف:** `components/sessions/session-popup.tsx` (السطر ~380)

### B1 — "كومبو" ← "كومب"
- **الحالة:** كان مُطبَّقًا بالفعل ✅ — لا توجد أي كلمة "كومبو" في أي مكون UI. جميع الإشارات في الواجهة تستخدم "كومب" (dropdown, settings, عرض السطور).

### B2 — القائمة المنسدلة للنوع الفرعي ديناميكية
- **الحالة:** كان مُطبَّقًا بالفعل ✅ — القائمة تتغير حسب play_type: عادي → فردي/مالتي، كومب → فردي/متولتة/مربعة. لا توجد خيارات "ثنائي" أو "رباعي".

### B3 — تحسين محاذاة أيقونة السهم في القوائم المنسدلة
- **المشكلة:** قائمة play-type كان لها `style` مخصص (paddingRight: 24px) لإفساح مساحة للسهم، لكن قائمة play-subtype كانت تفتقد هذه التعديلات — مما قد يسبب تداخل النص مع أيقونة السهم.
- **الإصلاح:** إضافة نفس الـ inline style إلى قائمة play-subtype (أيضًا: إخفاء appearance الافتراضي، paddingRight لمساحة السهم، direction: rtl).
- **الملف:** `components/sessions/session-popup.tsx` (السطر ~591)

### B4 — تسمية الجهاز في receipt popup
- **المشكلة:** receipt-popup.tsx كان يعرض النص الثابت "الجهاز" كلقب لمعلومات المحطة، بدلاً من عرض نوع المحطة الفعلي (بلياردو/بلايستيشن/بينغ بونغ).
- **الإصلاح:** استبدال النص الثابت بكود شرطي يحدد التسمية حسب `station.station_type` (نفس المنطق الموجود أصلاً في session-popup.tsx).
- **الملف:** `components/sessions/receipt-popup.tsx` (السطر 150)

### B5 — اختبار مباشر لتدفق "إضافة جيمات" (Live Test)
- **الإجراء:** تم تشغيل dev server، تسجيل الدخول بـ OWNER1، فتح جلسة بلياردو (وضع جيمات)، وإضافة دفعتين جيمات (normal/single, 3 games + combo/triple, 2 games)، ثم معاينة الإغلاق.
- **النتائج:**
  - ✅ دفعة normal/single (3 جيمات بسعر 7/جيم) → 21 ج.م
  - ✅ دفعة combo/triple (2 جيمات بسعر 20/جيم) → 40 ج.م
  - ✅ عرض الدفعات المسجلة عبر game-entries API → كلتا الدفعتين موجودة
  - ✅ الإجمالي في preview-close = 61 ج.م (21+40) — صحيح
  - ✅ تأكيد الإغلاق → `calculated_cost: 61`
- **السبب الجذري للفشل السابق:** كان مجرد مشكلة في تنسيق JSON في curl عبر PowerShell (هروب علامات الاقتباس)، وليس مشكلة في API نفسه. التدفق الفعلي يعمل بشكل صحيح.

### B6 — التحقق من عدم وجود ضرب سعري مضمّن
- **الحالة:** كان مُطبَّقًا بالفعل ✅ — جميع حسابات `games_count * price_per_game` تتم عبر دوال التسعير المركزية في `lib/pricing/calculation.ts` (calculateGameEntrySubtotal و calculateBilliardGameEntriesCost). لا يوجد أي ضرب مضمّن في session-popup.tsx.

---

### التحقق النهائي
- ✅ `npm run build` — ناجح بدون أخطاء TypeScript أو compilation
- ✅ لا يوجد أي `<input type="number">` في أي ملف بالمشروع
- ✅ لا يوجد أي ضرب سعري مضمّن خارج `lib/pricing/calculation.ts`
- ✅ تدفق إضافة الجيمات في البلياردو يعمل بشكل صحيح (تم اختباره مباشرة)

---

## يوليو 2026 — إصلاح start-session view للبلياردو (B-Cause)

### الخلفية
في التنفيذ الأصلي للمرحلة الخامسة (phase 5: billiard accumulated game entries)، تم تحديث **view الجلسة النشطة** فقط (قائمة الجيمات المسجلة + نموذج إضافة دفعة). أما **view بدء الجلسة** (available station) فبقي يعرضelements الـ legacy لجميع أنواع الأجهزة:
- "وضع اللعب: فردي/مالتي" لقيم mode
- "عدد الجيمات" input لحقل number

هذا تسبب في ظهور واجهة قديمة خاطئة عند محاولة بدء جلسة بلياردو من الإنتاج.

### الإصلاحات

#### 1. `components/sessions/session-popup.tsx` — UI start-session branching

تم إعادة هيكلة view بدء الجلسة ليتفرع حسب `station_type` و `billing_mode`:

| الحالة | المعروض |
|---|---|
| **بلياردو + Games** | فقط toggle طريقة الفوترة + رسالة "سيتم إضافة أنواع اللعب وعدد الجيمات بعد بدء الجلسة" + زر البدء |
| **بلياردو + Time** | toggle طريقة الفوترة + أزرار نوع اللعب (عادي/كومب) + أزرار النوع الفرعي الديناميكية (فردي/مالتي أو فردي/متولتة/مربعة) + زر البدء |
| **PS/Pingpong + Games** | الوضع القديم: toggle + وضع اللعب (فردي/مالتي) + عدد الجيمات + زر البدء |
| **PS/Pingpong + Time** | الوضع القديم: toggle + وضع اللعب (فردي/مالتي) + زر البدء |

تمت إضافة حالتي `startPlayType` و `startPlaySubtype` لحفظ اختيار نوع اللعب عند بدء جلسة بلياردو بالوقت.

#### 2. `components/sessions/session-popup.tsx` — handleStartSession branching

تم تعديل `handleStartSession` ليرسل:
- بلياردو+Games: `{ station_id, billing_mode }` فقط (بدون mode أو games_count)
- بلياردو+Time: `{ station_id, billing_mode, play_type, play_subtype }` بدلاً من mode
- PS/Pingpong: `{ station_id, mode, billing_mode, optional games_count }` كما كان

#### 3. `app/api/sessions/start/route.ts` — API validation branching

تم تعديل API:
- `mode` أصبح اختيارياً للبلياردو (مطلوب فقط لـ PS/Pingpong حيث لا يزال مستخدماً في التسعير)
- للبلياردو+Time: `play_type` و `play_subtype` مطلوبان
- للبلياردو+Games: لا mode ولا games_count ولا play_type/subtype مطلوبان
- `mode` يُشتق من `play_subtype` عند بدء جلسة بلياردو بالوقت (single → single, multi/triple/quad → multi)

#### 4. تم حذف `components/sessions/SessionPopupVerificationTest.tsx`
- ملف اختبار وهمي غير مستورد في أي مكان في التطبيق — تم تأكيد عدم وجود أي مرجع Import له

### التحقق اليدوي للحالات الأربع

1. ✅ **بلياردو + Games:** يعرض: toggle + رسالة توضيحية + زر البدء (لا mode, لا games_count)
2. ✅ **بلياردو + Time:** يعرض: toggle + أزرار عادي/كومب + أزرار فردي/مالتي/متولتة/مربعة + زر البدء
3. ✅ **PS + Games:** يعرض: toggle + أزرار فردي/مالتي + input عدد الجيمات + زر البدء (كما كان)
4. ✅ **PS + Time:** يعرض: toggle + أزرار فردي/مالتي + زر البدء (كما كان)

### نتائج التحقق
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ لا يوجد `<input type="number">` في أي ملف
- ✅ لا يوجد ضرب سعري مضمّن خارج calculation.ts

---

## يوليو 2026 — إصلاح bug حرج + مكوّن NumericInput الموحّد

### إصلاح bug حرج: "Failed to create session" عند بدء جلسة بلياردو+Games

**الجذر:** جلسات البلياردو+Games كانت تُنشأ بدون تعيين `mode` في جدول `sessions`، والعمود `mode TEXT NOT NULL` في قاعدة البيانات يرفض الإدراج بقيمة فارغة.

**سبب ظهوره:** التغيير السابق (بدء جلسة بلياردو بدون mode) أزال `mode` من الـ request body للبلياردو، لكن الـ API لم يضبط `mode` في `insertData` لجلسات البلياردو+Games. فقط جلسات البلياردو+Time كانت تضبط `mode` (مشتق من `play_subtype`).

**الإصلاح:** `app/api/sessions/start/route.ts` — إضافة else branch للبلياردو+Games يضبط `insertData.mode = "single"` (mode للبلياردو معلوماتي فقط — التسعير يتم عبر play_type/play_subtype لكل entry).

**التحقق المباشر:**
- ✅ بدء جلسة بلياردو+Games: `{"ok":true,"session":{"mode":"single",...}}`
- ✅ إضافة entry combo/triple/1 للسجل: `{"ok":true,"entry":{...}}`
- ✅ التدفق الكامل (start + add-game-entry) يعمل بنجاح
- ✅ التدفق بدون تحديد أولي (start only) يعمل بنجاح

### مكوّن NumericInput الموحّد

تم إنشاء `components/ui/numeric-input.tsx` ليحل محل جميع حقول الإدخال الرقمية الـ 30 في النظام، مما يوحّد سلوكها حسب الجهاز.

**السلوك:**
| الخاصية | سطح المكتب (pointer: fine) | الموبايل (pointer: coarse) |
|---------|---------------------------|---------------------------|
| `type` | `number` | `text` مع `inputMode="numeric"` |
| القيمة الافتراضية | `0` مرئي في الحقل | فارغ (يظهر placeholder) |
| أسهم الزيادة/النقص | نعم (native spinners) | لا |
| إرسال فارغ | يُرسل `0` | يُرسل `0` |

**الكشف عن الموبايل:** `window.matchMedia("(pointer: coarse)")` — يستمع للتغييرات عبر event listener.

**الملفات المعتمدة على NumericInput:**
- `components/sessions/session-popup.tsx` (5 حقول: startGamesCount, gamesCount, gamesCountInput, newEntryGamesCount, selectedQuantity)
- `components/expenses/expenses-list.tsx` (حقل المبلغ)
- `components/settings/ps-settings-form.tsx` (5 حقول: stationCount + 4 أسعار)
- `components/settings/pingpong-settings-form.tsx` (5 حقول: tableCount + 4 أسعار)
- `components/settings/billiard-settings-form.tsx` (11 حقول: tableCount + 10 أسعار)
- `components/settings/shifts-settings-form.tsx` (حقل عدد الورديات)
- `components/settings/products-settings-form.tsx` (حقلان: سعر المنتج الجديد + سعر التعديل)

**تغيير معماري:** 24 حالة state كانت تُخزّن أرقاماً كـ `string` (بسبب `useState(variable.toString() || "")`) وتم تحويلها إلى `number` لتتوافق مع `NumericInput` الذي يقبل `value: number`. تم تحديث دوال الحفظ (`parseFloat`/`parseInt` → المتغير مباشرة).

**نتائج التحقق:**
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ 0 `<input type="text" inputMode="numeric" pattern="[0-9]*">` متبقية في النظام
- ✅ 7 ملفات تستورد `NumericInput` ضمن `import { NumericInput } from "@/components/ui/numeric-input"`

## 2026-07-26 — NumericInput: لا أرقام افتراضية على الموبايل + Placeholders محدّدة

**المشكلة:** على أجهزة اللمس (mobile/touch)، كانت حقول NumericInput تظهر رقمًا افتراضيًا (0 أو 1) عند التحميل بدلاً من أن تبدأ فارغة بتعليمات إرشادية.

**الحل الجذري في `components/ui/numeric-input.tsx`:**
- تم إضافة حالة داخلية `mobileInput` تبدأ `""` دائمًا على الموبايل
- حقل الإدخال على الموبايل يُظهر دائمًا `mobileInput` فقط (فارغ عند البداية، يُملأ بكتابة المستخدم)
- التزامن مع القيمة الخارجية: عند إعادة تعيين النموذج إلى `0`، يتم مسح `mobileInput` تلقائيًا
- تم تغيير الـ default placeholder من `"أدخل العدد"` إلى `"أدخل القيمة"`

**تدقيق جميع مواقع الاستخدام (30 موقع):**
- `session-popup.tsx`: 5 مواقع — `placeholder="أدخل عدد الجيمات"` (4) + `"أدخل الكمية"` (1)
- `billiard-settings-form.tsx`: 11 موقع — `placeholder="أدخل عدد الطاولات"` (1) + `"أدخل السعر"` (10)
- `ps-settings-form.tsx`: 5 مواقع — `placeholder="أدخل عدد الأجهزة"` (1) + `"أدخل السعر"` (4)
- `pingpong-settings-form.tsx`: 5 مواقع — `placeholder="أدخل عدد الطاولات"` (1) + `"أدخل السعر"` (4)
- `products-settings-form.tsx`: موقعان — `placeholder="أدخل السعر"`
- `expenses-list.tsx`: موقع — `placeholder="أدخل المبلغ"`
- `shifts-settings-form.tsx`: موقع — `placeholder="أدخل عدد الورديات"`

**التحقق من clean-code-guard:** تم تمرير جميع الضوابط. تم إصلاح ازدواجية في `handleChange`.

**التحقق من test-guard:** لا يوجد كود اختبار متأثر — كل التغييرات في كود الإنتاج فقط.

## 2026-07-26 — إصلاحين لـ NumericInput على iPhone/Safari

**BUG 1 — تكبير الشاشة عند التركيز على الحقل (Safari auto-zoom):**
- السبب: iOS Safari يقوم بتكبير الصفحة تلقائيًا عند التركيز على أي حقل إدخال يكون حجم الخط فيه أقل من 16px.
- الحل: تم إضافة `style={{ fontSize: '16px' }}` إلى إصدار اللمس (touch) في `numeric-input.tsx`، مما يضمن أن يكون حجم الخط 16px على الأقل بغض النظر عن أي `className` يتم تمريره من المواقع المستخدمة (مثل `text-xs`).
- لا تم استخدام meta viewport zoom=no لأنه يضر بإمكانية الوصول — تم إصلاح السبب الجذري (حجم الخط).

**BUG 2 — أرقام عربية-هندية بدل الغربية (Arabic-Indic numerals):**
- السبب: في بعض الأجهزة ذات الإعدادات العربية، يُظهر `inputMode="numeric"` لوحة أرقام ١٢٣ بدلاً من 123.
- الحل: تم إضافة `lang="en"` إلى عنصر `<input>` في إصدار اللمس. هذا يخبر Safari باستخدام الأرقام الغربية (Western digits) للوحة المفاتيح الرقمية بغض النظر عن إعدادات الجهاز الإقليمية. لا يؤثر `lang="en"` على اتجاه النص العام للصفحة لأنه مقتصر على هذا العنصر فقط.

**التحقق من clean-code-guard:** نظيف — لا مخالفات.
**التحقق من test-guard:** غير قابل للتطبيق — لا يوجد كود اختبار متأثر.

## 2026-07-26 — Fix: Popup overflow scroll + Remove game entry button

**BUG 1 — Popup overflow no scroll on mobile:**
- **المشكلة:** محتوى session-popup و receipt-popup (جلسات + مشروبات) يزيد عن طول الشاشة على الموبايل ولا يوجد سكرول للوصول لزر الإجراء (الإنهاء).
- **الحل في `session-popup.tsx`:**
  - تغيير `card` div من `p-6` إلى `flex flex-col max-h-[85vh]`
  - الهيدر: `shrink-0 px-6 pt-6 pb-0` (ثابت لا يتقلص)
  - المحتوى القابل للسكرول: لفّ في `overflow-y-auto flex-1 px-6 pb-6`
- **الحل في `receipt-popup.tsx`:** نفس النمط مطبق.

**BUG 2 — No way to remove billiard game entry:**
- إنشاء `app/api/sessions/remove-game-entry/route.ts` — DELETE endpoint maps remove-product pattern:
  - Query: `?entry_id=xxx`
  - يتحقق من `getShopIdFromRequest`
  - يتحقق من وجود `entry_id`
  - يستعلم الـ entry مع `sessions!inner` للتحقق من نشاط الجلسة
  - يحذف السجل من `billiard_game_entries` مع `shop_id`
- إضافة state `removingEntryId` وإضافة `handleRemoveGameEntry` في `session-popup.tsx`
- إضافة زر حذف (×) أحمر لكل صف جيمات — نفس تصميم زر حذف المشروبات

**نتائج التحقق:**
- ✅ `npm run build` — ناجح بدون أخطاء
- ✅ session-popup: محتوى الجلسات قابل للسكرول
- ✅ receipt-popup: محتوى الإيصال قابل للسكرول
- ✅ API remove-game-entry: 404, 409, 400, 500 معالجة
- ✅ زر حذف (×) يظهر لكل جيم مع loading spinner
- ✅ toast نجاح/فشل بعد كل عملية حذف
- ✅ إجمالي الجيمات يتحدّث تلقائياً بعد الحذف

**ملفات التنفيذ:**
- `components/sessions/session-popup.tsx` — سكرول + زر حذف + handler
- `components/sessions/receipt-popup.tsx` — سكرول
- `app/api/sessions/remove-game-entry/route.ts` — API جديد

**التحقق من clean-code-guard:** نظيف — لا مخالفات.
**التحقق من test-guard:** غير قابل للتطبيق — لا يوجد كود اختبار متأثر.

