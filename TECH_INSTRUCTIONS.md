# تعليمات المشروع والتكنولوجيا المستخدمة — نظام إدارة محل بلايستيشن

هذا الملف يُقرأ **مع** ملف `BRD_PS_System.md` قبل أي كتابة كود. الملف الأول يوصف "المطلوب"، وهذا الملف يوصف "إزاي هنبنيه".

---

## 1) الـ Tech Stack

| الطبقة | الاختيار | السبب |
|---|---|---|
| Frontend + Backend | **Next.js (React)** | فرونت وباك في مشروع واحد، بدون تعارض CORS، وأنواع بيانات مشتركة بينهم |
| التنسيق | **Tailwind CSS** | بناء واجهات Responsive بسرعة من أول سطر كود |
| قاعدة البيانات | **PostgreSQL عبر Supabase** | قاعدة علائقية + Auth + Realtime + تخزين ملفات في مكان واحد، مع باقة مجانية كافية للبدء |
| الاستضافة | **Vercel** (فرونت) + **Supabase** (باك/بيانات) | نشر تلقائي مع كل push، مناسب للتجربة الفعلية بدري |

---

## 2) قرارات معمارية أساسية (لازم تُحترم أثناء الكتابة)

1. **لا يوجد فرونت اند وباك اند منفصلين في مشروعين** — كل حاجة داخل مشروع Next.js واحد.
2. **حساب الوقت مصدره السيرفر لا المتصفح.** `start_time` و`end_time` يُسجَّلان في قاعدة البيانات لحظة الضغط، والعداد الظاهر للمستخدم تمثيل بصري فقط، والتكلفة النهائية تُحسب من الفرق المسجَّل في السيرفر.
3. **قاعدة البيانات جاهزة للتعدد منذ اليوم الأول.** كل جدول (ما عدا `shops` نفسه) يحمل `shop_id`، حتى لو يوجد صف واحد فقط في `shops` حاليًا. هذا يسهّل التوسع لنظام متعدد العملاء لاحقًا دون إعادة بناء.
4. **لا صفحة إعداد إجبارية عند أول دخول.** بعد تسجيل الدخول بالـ ID، المستخدم يدخل مباشرة للنظام، والإعدادات صفحة داخلية يدخلها وقت ما يحب.
5. ~~**قفل التعديل أثناء الوردية المفتوحة.** أي محاولة تعديل أسعار أو إعدادات ووردية مفتوحة، يجب أن تُرفض من الـ API نفسه (مش بس تُخفى من الواجهة).~~ (أُلغي هذا القرار — يوليو 2026). لا يوجد حاليًا أي قفل على تعديل الأسعار أو الإعدادات أثناء وجود وردية مفتوحة — أي مستخدم مسجّل دخول يقدر يعدّل في أي وقت، بغض النظر عن حالة الوردية. هذا القرار مؤقت لحين بناء الفيتشر 2 (الفريق والصلاحيات)، حيث سيُحدَّد وقتها هل صاحب المحل يقدر يمنح أو يمنع صلاحية التعديل لكل موظف بشكل فردي. ملاحظة: هذا الإلغاء يخص قفل التعديل على الإعدادات فقط.
6. **كل جهاز (بلايستيشن/بلياردو/بينغ بونغ) له سجل مستقل في جدول `stations`، وليس عدد إجمالي في `shops`.** لما صاحب المحل يحدد "عندي 5 أجهزة بلايستيشن" في الإعدادات، لازم النظام ينشئ/يحدّث **5 صفوف فعلية** في `stations` (كل صف بمعرّف مستقل، حالة تفعيل، اسم قابل للتعديل)، مش عمود رقمي واحد على `shops`. السبب: الشاشة الرئيسية محتاجة تعرض **كارت مستقل لكل جهاز بحالته الفعلية (شغال/فاضي)**، ومحرك الجلسات (`sessions`) محتاج يربط كل جلسة بجهاز محدد عن طريق `station_id`. أي أعمدة عدّ بسيطة (`ps_station_count` ونحوها) على `shops` تُعتبر حلاً مؤقتًا غير مكتمل ويجب استبدالها بصفوف `stations` فعلية قبل بناء محرك الجلسات.
7. **كل جهاز، أيًا كان نوعه (بلايستيشن/بلياردو/بينغ بونغ)، يحمل وضعين للتسعير معًا في نفس الوقت داخل `pricing_rules`:** سعر بالوقت (فردي/مالتي) + سعر بالجيم (فردي/مالتي). ده تغيير عن القرار القديم اللي كان بيربط كل نوع جهاز بوضع تسعير واحد بس. الوضع الفعلي المطبَّق على كل جلسة يتحدد بعمود `billing_mode` على جدول `sessions` (قيمته `'time'` أو `'games'`)، ويُختار من الموظف لحظة فتح الجلسة عبر سويتش في بوب أب الجلسة، ويثبت طول عمر الجلسة (زي فردي/مالتي بالظبط — لا يتغيّر بعد البدء).
8. **إضافة الموظف بالإيميل لا تُغيّر معمارية المصادقة الأساسية.** حقل الإيميل يُستخدم فقط وقت إضافة الأدمن للموظف، لتوليد `login_id` تلقائي وحفظه كبيانات تعريفية. تسجيل الدخول الفعلي للنظام يفضل بالـ `login_id` فقط، بدون كلمة سر، بدون Supabase Auth — القرار المعماري رقم 4 أعلاه لا يتغيّر بأي شكل.
9. **حذف الموظف (soft delete) يخفيه تمامًا من الواجهة، ويُحذف نهائيًا بعد 30 يوم ضمن نفس مهمة فيتشر 8.** لما الأدمن يمسح موظف، `is_active` يبقى `false` وعمود جديد `deactivated_at` يتسجّل بتوقيت السيرفر لحظة الحذف. الموظف ده **ما يظهرش في أي واجهة خالص** (لا في صفحة إدارة الفريق ولا أي مكان تاني — لا يوجد قسم "أعضاء سابقون" في الواجهة). بياناته تفضل في قاعدة البيانات بس للحفاظ على ترابط الورديات/الجلسات التاريخية، وتتحذف نهائيًا تلقائيًا بعد 30 يوم بالضبط من `deactivated_at` — عبر **نفس المهمة المجدولة اليومية** المبنية لحذف الورديات القديمة في فيتشر 8، مش مهمة منفصلة.

---

## 3) هيكل الجداول (ملخص — التفاصيل الكاملة تُبنى في `supabase/migrations`)

- `shops` — بيانات المحل (اسم، شعار)
- `users` — الفريق (صاحب محل/موظف) مرتبطين بـ `shop_id`، يحمل عمود `email` (تعريف/دعوة وقت الإضافة فقط، ليس وسيلة دخول) و`login_id` (وسيلة الدخول الوحيدة، قابل للتعديل من الأدمن) و`deactivated_at` (توقيت الحذف الفعلي، يُستخدم لحساب النافذة المتحركة 30 يوم قبل الحذف النهائي)
- `stations` — أجهزة البلايستيشن وطاولات البلياردو والبينغ بونغ (كل جهاز صف مستقل، وليس عدد إجمالي)
- `pricing_rules` — تسعيرة كل جهاز، تحمل **الوضعين معًا** بغض النظر عن نوع الجهاز: سعر بالساعة (فردي/مالتي) + سعر بالجيم (فردي/مالتي)
- `sessions` — الجلسات الفعلية (وقت البداية/النهاية والتكلفة المحسوبة)، يحمل عمود `billing_mode` (`'time'` | `'games'`) يتحدد لحظة فتح الجلسة ويثبت طول عمرها
- `products` — المشروبات وأسعارها
- `sale_items` — مبيعات المشروبات المرتبطة بالجلسات
- `shifts` — الورديات (اسم المسؤول، وقت الفتح/القفل)
- `expenses` — المصاريف اليومية

---

## 4) هيكل الفولدرات

```
ps-shop-system/
├── app/
│   ├── (auth)/            → تسجيل الدخول بالـ ID
│   ├── dashboard/          → الشاشة الرئيسية (الأجهزة، الوردية الحالية)
│   ├── settings/            → الأسعار، المكوّنات، عدد الورديات، المشروبات
│   ├── history/             → أرشيف آخر 30 يوم
│   └── api/                 → نقاط الاتصال بقاعدة البيانات
│       ├── sessions/
│       ├── shifts/
│       └── products/
├── components/              → عناصر واجهة قابلة لإعادة الاستخدام
├── lib/                     → منطق الحسابات (تسعير الوقت) + اتصال Supabase
├── types/                   → تعريفات الأنواع المشتركة بين الفرونت والباك
└── supabase/migrations/     → ملفات إنشاء الجداول
```

---

## 5) خطوات البناء بالترتيب

1. تجهيز مشروع Next.js + ربطه بـ Supabase + إنشاء الجداول (صف واحد في `shops`)
2. صفحة الإعدادات: الأسعار، المكوّنات، المشروبات
3. محرك الجلسات: بدء/إيقاف الجهاز، فردي/مالتي، حساب التكلفة، عدّاد جيمات البلياردو
4. الورديات: فتح/قفل، اسم المسؤول، عدد الورديات القابل للتحديد
5. الفريق والصلاحيات: إضافة/حذف، وتحديد صلاحية التعديل لكل موظف (بما فيها إعادة النظر في قفل الوردية كجزء من نظام الصلاحيات)
6. اختبار Responsive على كل شاشة، ونشر على Vercel من أول أسبوع للتجربة الفعلية

---

## 6) نظام الألوان والخطوط (Design Tokens)

الوضع الافتراضي **Dark Mode فقط** في هذه المرحلة، بالألوان التالية (بدون أي تدرج/Gradient — ألوان صلبة فقط):

| الاستخدام | اللون |
|---|---|
| خلفية الصفحة الأساسية | `#1A1C23` |
| خلفية الكروت والعناصر (Cards) | `#252830` |
| اللون الأساسي (أزرار، روابط، تمييز) | `#00AEFF` |
| النص الأساسي | `#FFFFFF` |
| النص الثانوي/الوصفي | رمادي فاتح متوسط تقريبًا `#A0A3AC` |

**قاعدة صارمة:** ممنوع استخدام `gradient` في أي زرار أو خلفية — اللون الأزرق `#00AEFF` يُستخدم صلبًا (solid) في كل مكان.

### الخط الرسمي للنظام

- **الخط المعتمد لكل النظام (عربي وإنجليزي/أرقام):** `IBM Plex Sans Arabic`
- يُحمَّل عبر `next/font/google` (متاح على Google Fonts) وليس عبر رابط CDN خارجي أو ملفات خط يدوية، للحفاظ على الأداء وتجنب مشاكل الـ FOUT/FOIT.
- يُعرَّف مرة واحدة في `app/layout.tsx` (أو `tailwind.config.ts` كـ `fontFamily.sans`) ويُستخدم كخط افتراضي لكل عناصر الواجهة — **ممنوع استخدام أي خط تاني (زي الخط الافتراضي للمتصفح أو Arial) في أي مكوّن**.
- لازم يدعم الأوزان المطلوبة للتصميم (على الأقل: Regular / Medium / SemiBold / Bold) عشان التمييز البصري بين العناوين والنصوص العادية والأزرار.

---

## 6.5) شريط النظام العلوي (Top Bar) — قرار نهائي

هيكل الشريط العلوي الثابت في كل صفحات النظام بعد تسجيل الدخول (`components/layout/authenticated-shell.tsx`):

| الناحية | العنصر | الوصف |
|---|---|---|
| أقصى الشمال | **"PS-System"** | اسم المنتج/النظام نفسه، نص ثابت **غير تفاعلي** (مش زرار ومالوش dropdown)، ولا يتغيّر بتغيّر العميل — دي هوية المنتج الثابتة، مهمة خصوصًا وقت التحول لـ SaaS متعدد العملاء لاحقًا |
| أقصى اليمين | **اسم المحل** (`shops.name`) | ديناميكي لكل عميل (مختلف من محل للتاني)، **وهو نفسه الزرار (Trigger)** اللي بيفتح القائمة المنسدلة — مش عنصر منفصل عن زرار مستخدم قديم |

**قواعد صارمة:**
1. اسم المحل يجب أن يبان بصريًا إنه قابل للنقر (Clickable): سهم صغير (▾) بجانبه على الأقل، بالإضافة لتأثير Hover واضح (تغيير خفيف في الخلفية مثلاً).
2. القائمة المنسدلة (تفتح من الضغط على اسم المحل) تحتفظ بنفس المحتوى الموجود حاليًا: اسم المستخدم، معرّف الدخول (`login_id`)، رابط الإعدادات، زر تسجيل الخروج. **فقط نقطة التفعيل (Trigger) هي اللي بتتغيّر**، مش المحتوى.
3. **لا يوجد زرار مستخدم منفصل على الشمال بعد الآن** — تم استبداله بالكامل باسم المحل نفسه على اليمين كنقطة التفعيل الوحيدة.
4. السبب المعماري: اسم المحل وبيانات الحساب/الجلسة (المستخدم، الإعدادات، الخروج) مرتبطين منطقيًا ببعض — كلاهما "سياق الجلسة الحالية" (مين أنا، وفي أنهي محل). لذلك يجب تجميعهم في نفس الناحية (اليمين) بدل تفريقهم. بينما "PS-System" هوية المنتج المجردة تقعد لوحدها في الناحية التانية (الشمال) كعنصر ثابت لا علاقة له بالجلسة الحالية.

**الملف المتأثر بهذا التعديل:** `components/layout/authenticated-shell.tsx`

**تحديث (يوليو 2026) — لقب الدور في القائمة المنسدلة:** بجانب اسم المستخدم و`login_id` داخل القائمة المنسدلة، يجب عرض لقب واضح يوضّح دور الحساب الحالي المسجَّل دخوله: **"مدير"** لو `role = 'owner'`، أو **"موظف"** لو `role = 'staff'`. الهدف: يبقى واضح فورًا لأي مستخدم (خصوصًا وقت اختبار أكتر من حساب في أكتر من تاب) هو داخل بحساب مين وبأنهي صلاحية، من غير ما يحتاج يروح لصفحة إدارة الفريق.

**تحديث (يوليو 2026) — مصدر واحد لاسم الأونر، وإزالة زر التعديل من صف الأونر في إدارة الفريق:**
- **اسم صاحب المحل مصدره الوحيد `shops.owner_name`**، ويُعدَّل حصريًا من صفحة الإعدادات الرئيسية (فيتشر 7). لا يُسمح بتعديله من صفحة "إدارة الفريق" ولا من أي مكان تاني، تجنبًا لوجود مصدرين مختلفين لنفس الاسم.
- القائمة المنسدلة في الشريط العلوي: لو `role = 'owner'`، الاسم المعروض جنب اللقب "مدير" لازم يُجلب من `shops.owner_name` (مش من `users.display_name`). لو `role = 'staff'`، الاسم المعروض من `users.display_name` زي ما هو.
- في صفحة "إدارة الفريق"، **صف الأونر لا يظهر له زر "تعديل" ولا زر "حذف" خالص** — الأونر أصلاً معاه كل الصلاحيات بشكل غير مشروط (زي ما هو موثّق في `assertPermission`)، فمفيش داعي يعدّل صلاحياته لنفسه، ومينفعش يتحذف. زرارَي "تعديل"/"حذف" يظهروا فقط لصفوف الموظفين (`role = 'staff'`).

---

## 6.6) تصميم بوب أب تشغيل الجهاز (Session Popup) — قرار نهائي

هذا التصميم مرتبط بـ`BRD_PS_System.md` فيتشر 5 (User Stories الخاصة بتشغيل/إيقاف الأجهزة)، ويُطبَّق عند بناء محرك الجلسات (الخطوة 3 من القسم 5 أعلاه).

**الشاشة الرئيسية:**
- كارت مستقل لكل جهاز فعلي (صف في جدول `stations`)، عدد الكروت = عدد صفوف `stations` الفعّالة لكل `station_type`.
- كل كارت فيه **أيقونة تعديل (✏️) منفصلة** — الدوس عليها يفتح بوب أب صغير مستقل (غير بوب أب الجلسة) فيه حقل نصي باسم الجهاز الحالي وزرار "تأكيد". التعديل هنا **لا يفتح بوب أب الجلسة ولا يؤثر عليه** — مجرد تغيير اسم (`stations.name`) بس.
- الكارت يعرض الحالة الحالية للجهاز: **فاضي** أو **شغال** (بناءً على وجود صف `sessions` بحالة `status = 'active'` مرتبط بنفس `station_id`).

**سلوك الضغط على الكارت (بوب أب واحد بحالتين):**

| حالة الجهاز | محتوى البوب أب |
|---|---|
| **فاضي** | بدء جلسة جديدة: أولًا اختيار **وضع الفوترة** (بالوقت / بعدد الجيمات) عبر سويتش، بعدها اختيار فردي/مالتي، زرار "ابدأ" |
| **شغال** | إضافة مشروبات على الجلسة الجارية، زرار "إنهاء الوقت" (أو "إنهاء الجيمات" حسب `billing_mode` الجلسة) |

**قواعد سلوكية صارمة:**
1. فتح/قفل البوب أب (بدون الضغط على "إنهاء الوقت") **لا يوقف الجلسة ولا يؤثر عليها إطلاقًا** — الوقت يستمر في الخلفية بناءً على `start_time` في قاعدة البيانات فقط، بغض النظر عن حالة الشاشة أو المتصفح (تطبيق مباشر لقرار 2 في القسم 2 أعلاه). ينطبق نفس المبدأ بغض النظر عن `billing_mode`.
2. إضافة مشروبات على الجلسة تُسمح **فقط أثناء كون الجلسة نشطة** (`status = 'active'`)، أي قبل الضغط على "إنهاء الوقت".
3. **وضع الفوترة (`billing_mode`) يتحدد مرة واحدة بس لحظة الضغط على "ابدأ"**، ويُكتب في `sessions.billing_mode` عند إنشاء الصف، ولا يوجد أي مسار في الواجهة أو الـ API يسمح بتغييره بعد كده لنفس الجلسة (نفس القاعدة المطبّقة على فردي/مالتي).
4. الضغط على "إنهاء الوقت" **يحسب فقط ولا يسجّل أي شيء في قاعدة البيانات.** يُستدعى endpoint للحساب (read-only، بدون أي UPDATE) يحسب حسب `billing_mode` الجلسة: لو `time` يحسب المدة المفترضة لو انتهت الآن، ولو `games` يحسب على أساس عدد الجيمات المسجّلة، بالإضافة لتكلفة المشروبات المضافة، والإجمالي — ويعرضها في بوب أب الإيصال. **حالة الجلسة تظل `active` ولا يتغيّر `end_time` في هذه الخطوة.**
5. لو المستخدم رجع من بوب أب الإيصال **من غير** ما يضغط "تأكيد القفل"، الجلسة تفضل شغالة تمامًا (`active`) والوقت (أو عداد الجيمات) يستمر يتحسب طبيعي — كأن حاجة ما حصلتش خالص.
6. الجلسة **تُقفل فعليًا فقط** عند الضغط الصريح على "تأكيد القفل" داخل بوب أب الإيصال — وهنا فقط يُستدعى endpoint الكتابة (`end_time = NOW()`, `status = 'completed'`) بتوقيت السيرفر لحظة الضغط الفعلي (مش توقيت لحظة فتح الإيصال، لأن المستخدم ممكن ياخد وقت قبل ما يأكد).
7. بعد التأكيد، الكارت في الشاشة الرئيسية يرجع لحالة "فاضي".

**ملفات متوقعة:** `components/sessions/station-card.tsx`، `components/sessions/session-popup.tsx` (يشمل سويتش وضع الفوترة)، `components/sessions/receipt-popup.tsx`، `app/api/sessions/start/route.ts` (يستقبل `billing_mode` عند الإنشاء)، `app/api/sessions/add-product/route.ts`، `app/api/sessions/preview-close/route.ts` (حساب فقط، بدون كتابة، يتفرّع حسب `billing_mode`)، `app/api/sessions/confirm-close/route.ts` (الكتابة الفعلية وقفل الجلسة).

---

## 6.7) إضافة الموظف بالإيميل وتوليد `login_id` — قرار نهائي

هذا التصميم مرتبط بـ `BRD_PS_System.md` فيتشر 2 (إدارة الفريق والصلاحيات)، ويُطبَّق عند بناء هذا الفيتشر (الخطوة 5 من القسم 5 أعلاه).

**الفلو:**
1. الأدمن يفتح صفحة إدارة الفريق ويدخل إيميل الموظف الجديد.
2. يدوس "تأكيد".
3. النظام يولّد `login_id` تلقائي (تُحدَّد صيغة التوليد بالتفصيل وقت التنفيذ الفعلي — رقم متسلسل أو معرّف قصير غير قابل للتخمين، بما يتوافق مع الملاحظة الأمنية في فيتشر 1) ويحفظ صف جديد في `users` بـ `email` + `login_id` المولّد + `shop_id`.
4. الـ `login_id` يظهر للأدمن فورًا بعد التأكيد عشان يقدر يديه للموظف.

**قواعد صارمة:**
1. الإيميل **لا يُستخدم كوسيلة دخول** — الدخول الفعلي بالـ `login_id` فقط، بدون كلمة سر، بدون Supabase Auth (لا تغيير على القرار المعماري رقم 4 في القسم 2 أعلاه).
2. الأدمن يقدر يعدّل `login_id` أي موظف في أي وقت بعد التوليد (تعديل مباشر من صفحة إدارة الفريق).
3. الأدمن يقدر يمسح أي موظف نهائيًا من الواجهة، وده لازم يقفل صلاحية الدخول بتاعه فورًا. تقنيًا هذا **soft delete** (`users.is_active = false`) وليس حذفًا فعليًا فورًا — الحفاظ على الترابط مع الورديات/الجلسات القديمة المرتبطة بيه. **الموظف المعطَّل (`is_active = false`) لا يظهر في واجهة صفحة إدارة الفريق إطلاقًا بعد الحذف** — لا يوجد قسم "أعضاء سابقون" ولا أي عرض له، رغم بقاء صفه في قاعدة البيانات حتى موعد الحذف النهائي (راجع فيتشر 8 في BRD للتفاصيل الكاملة لمنطق الحذف النهائي التلقائي بعد 30 يوم).
4. تعديل الصلاحيات لكل موظف (تفاصيلها التفصيلية جزء من نفس الفيتشر) لازم يتم من نفس صفحة إدارة الفريق.

**ملفات متوقعة:** `app/settings/team/page.tsx` (أو المسار المكافئ لصفحة إدارة الفريق)، `app/api/team/invite/route.ts` (إضافة موظف بالإيميل وتوليد `login_id`)، `app/api/team/[id]/route.ts` (تعديل `login_id`/الصلاحيات، أو حذف الموظف).

---

## 7) تعليمات لأداة الكود (قبل كتابة أي سطر كود)

عند بدء أي جلسة عمل مع الأداة (Cursor / Claude Code / أي أداة تانية):

1. اقرأ `BRD_PS_System.md` وهذا الملف كاملين.
2. لا تكتب أي كود بعد.
3. لخّص فهمك للمشروع، واقترح خطة تنفيذ خطوة بخطوة بنفس ترتيب القسم رقم 5 أعلاه.
4. انتظر التأكيد قبل البدء الفعلي في الكتابة.

**عند الانتقال لأداة جديدة (بعد نفاد الحد المسموح في أداة سابقة) ويوجد كود مكتوب بالفعل:**
1. اطلب من الأداة فتح المشروع وقراءة الكود الموجود و git log بنفسها.
2. أعطها هذا الملف و`BRD_PS_System.md`.
3. أعطها ملف `PROGRESS.md` (يُنشأ عند بدء الكتابة الفعلية، ويُحدَّث بعد كل فيتشر يكتمل، ويحتوي على: ما تم بناؤه، القرارات البرمجية أثناء الكتابة، والخطوة التالية).
4. اطلب منها تلخيص فهمها قبل الاستكمال.

---

## 8) نقاط تحقق إلزامية — أخطاء متكررة لازم نحذر منها

هذه أخطاء **حدثت فعليًا** أثناء العمل على هذا المشروع، وليست افتراضية. أي أداة كود يجب أن تلتزم بالتالي:

| القاعدة | السبب |
|---|---|
| **"تمت الكتابة" لا تعني "تم التطبيق"** — أي تعديل على قاعدة البيانات (migration) يجب التحقق من نجاحه بفحص مباشر على Supabase الحقيقي، لا يكفي افتراض أن الكود المكتوب محليًا قد نُفِّذ | حدث أن migration كامل كُتب على الجهاز ولم يُطبَّق فعليًا على القاعدة الحقيقية |
| **لا تفترض أسماء أعمدة أو متغيرات من الذاكرة** — تحقق فعليًا (عبر `information_schema.columns` أو فتح الملف نفسه) قبل كتابة أي كود يعتمد عليها | حدث افتراض عمود `name` بينما الاسم الفعلي كان `display_name` |
| **ممنوع أي حل "مؤقت للاختبار"** (bypass، حساب وهمي، تخطي تحقق) — أي فيتشر أساسي في BRD يُبنى بشكل حقيقي من البداية | الحلول المؤقتة تُنسى وتبقى في الكود كثغرة أمنية دائمة |
| **مصدر واحد فقط لأي منطق حساس** (تشفير، صلاحيات، تسعير) — لا يُكتب نفس المنطق مرتين بنسختين "متشابهتين"، بل يُستورد من مكان واحد أو تُعدَّل الدالة الأصلية لتعمل في كل السياقات المطلوبة | حدث اقتراح كتابة نسخة ثانية من منطق توقيع الجلسة بدلًا من إعادة استخدام نفس الدالة |
| **تأكد من العمل داخل جذر المشروع الصحيح** (المجلد الذي يحتوي على `package.json`) قبل تنفيذ أي أمر طرفية | حدث تنفيذ أمر من المجلد الأب الخاطئ فأعطى خطأ ENOENT |
| **العمليات التي يجب أن تبقى تعمل (مثل `npm run dev`) تُشغَّل يدويًا في نافذة طرفية منفصلة يبقيها المستخدم مفتوحة**، لا تُشغَّل كعملية خلفية من داخل جلسة الأداة | حدث توقف السيرفر تلقائيًا بعد انتهاء جلسة الأداة التي شغّلته |
| **`git status` يجب أن يكون نظيفًا قبل بدء أي مرحلة جديدة** — كل تصحيح أو مرحلة له commit منفصل وواضح | يسهّل الرجوع لأي نقطة لاحقًا ويمنع خلط التصحيحات بالفيتشرز الجديدة |
| **عند تلخيص الفهم قبل الكتابة، يجب أن يتضمن الملخص تفاصيل حقيقية** (أسماء ملفات فعلية، أرقام commits فعلية) لا كلامًا عامًا — إن كان الملخص عامًا جدًا، هذا مؤشر على أن الملفات لم تُقرأ فعليًا | يكشف ما إذا كانت الأداة قرأت المشروع فعلًا أم تخمّن |
